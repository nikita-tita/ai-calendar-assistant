# Настройка домена этонесамыйдлинныйдомен.рф на REG.RU VPS

**Дата:** 10 октября 2025
**Статус:** Веб-приложение развёрнуто, требуется настройка DNS

---

## ✅ Что уже сделано

### 1. Nginx установлен и настроен
```bash
✅ Nginx 1.18.0 - установлен
✅ Certbot - установлен
✅ Конфигурация для домена - создана
✅ Веб-приложение загружено в /var/www/calendar/
```

### 2. Сайт работает по IP
- **URL:** http://95.163.227.26
- **Статус:** 200 OK
- **Приложение:** AI Calendar WebApp

---

## 🔧 Что нужно сделать

### Шаг 1: Настроить DNS записи в REG.RU

Вам нужно зайти в панель управления доменом на REG.RU и добавить A-записи:

#### Войдите в панель REG.RU
1. Откройте https://www.reg.ru
2. Войдите в личный кабинет
3. Перейдите к управлению доменом **этонесамыйдлинныйдомен.рф**

#### Добавьте A-записи DNS

Найдите раздел "DNS-серверы и управление зоной" или "Управление DNS":

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | @ | 95.163.227.26 | 3600 |
| A | www | 95.163.227.26 | 3600 |

Где:
- **@** - означает корневой домен (этонесамыйдлинныйдомен.рф)
- **www** - поддомен www.этонесамыйдлинныйдомен.рф
- **95.163.227.26** - IP адрес вашего VPS сервера
- **3600** - время кэширования (1 час)

#### Сохраните изменения
После сохранения DNS записи могут обновляться от 5 минут до 24 часов (обычно 15-30 минут).

---

### Шаг 2: Проверить работу DNS

После настройки DNS подождите 15-30 минут и проверьте:

#### Через командную строку (на вашем Mac):
```bash
# Проверить, указывает ли домен на правильный IP
nslookup этонесамыйдлинныйдомен.рф

# Или через dig
dig этонесамыйдлинныйдомен.рф +short
```

Должно вернуться: **95.163.227.26**

#### Через браузер:
Откройте: http://этонесамыйдлинныйдомен.рф

Если видите приложение - DNS настроен правильно!

---

### Шаг 3: Установить SSL сертификат (HTTPS)

⚠️ **ВАЖНО:** Выполняйте этот шаг ТОЛЬКО после того, как DNS заработает!

После того как домен начнёт открываться по HTTP, установите SSL:

```bash
# Подключитесь к серверу
ssh root@95.163.227.26

# Установите SSL сертификат
certbot --nginx -d этонесамыйдлинныйдомен.рф -d www.этонесамыйдлинныйдомен.рф

# Или через Punycode (если кириллица не работает):
certbot --nginx -d xn--80aaagmglcejiocg0afgqeuc4azf.xn--p1ai -d www.xn--80aaagmglcejiocg0afgqeuc4azf.xn--p1ai
```

**Certbot автоматически:**
- Получит бесплатный SSL сертификат от Let's Encrypt
- Настроит Nginx для HTTPS
- Настроит автоматическое перенаправление с HTTP на HTTPS
- Настроит автопродление сертификата

После этого ваш сайт будет доступен по:
- ✅ https://этонесамыйдлинныйдомен.рф (с SSL)

---

## 📋 Текущая архитектура

```
REG.RU VPS (95.163.227.26)
│
├── Docker: telegram-bot (порт внутренний)
│   └── AI Calendar Bot для Telegram
│
├── Docker: radicale (порт 5232)
│   └── CalDAV календарь
│
└── Nginx (порт 80, 443)
    └── Веб-приложение /var/www/calendar/
        └── AI Calendar WebApp
```

**Все работает на ОДНОМ сервере REG.RU!**

---

## 🔍 Проверка статуса

### Проверить, работает ли Nginx
```bash
ssh root@95.163.227.26
systemctl status nginx
```

### Проверить логи Nginx
```bash
ssh root@95.163.227.26
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### Проверить конфигурацию Nginx
```bash
ssh root@95.163.227.26
cat /etc/nginx/sites-available/calendar
```

### Обновить веб-приложение
Если вам нужно обновить index.html:
```bash
# На вашем Mac
cd /Users/fatbookpro/calendar-webapp
sshpass -p '$SERVER_PASSWORD' rsync -avz index.html root@95.163.227.26:/var/www/calendar/
```

---

## 🚨 Решение проблем

### Сайт не открывается по домену
1. Проверьте DNS: `nslookup этонесамыйдлинныйдомен.рф`
2. Подождите 30 минут - DNS может кэшироваться
3. Попробуйте очистить кэш DNS на вашем компьютере:
   ```bash
   # macOS
   sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder
   ```

### Certbot выдаёт ошибку
- Убедитесь, что DNS настроен и домен открывается по HTTP
- Проверьте, что порт 80 открыт: `telnet 95.163.227.26 80`
- Попробуйте использовать Punycode версию домена

### Nginx не запускается
```bash
# Проверить конфигурацию
nginx -t

# Посмотреть ошибки
journalctl -u nginx -n 50
```

---

## 📞 Полезные команды

### Перезапустить Nginx
```bash
ssh root@95.163.227.26
systemctl reload nginx  # Мягкая перезагрузка (без разрыва соединений)
systemctl restart nginx # Полная перезагрузка
```

### Проверить SSL сертификат
```bash
# После установки SSL
openssl s_client -connect этонесамыйдлинныйдомен.рф:443 -servername этонесамыйдлинныйдомен.рф
```

### Автопродление SSL
Certbot автоматически настроил таймер для продления:
```bash
# Проверить статус таймера
systemctl status certbot.timer

# Проверить продление вручную (dry-run)
certbot renew --dry-run
```

---

## ✅ Финальная проверка

После настройки DNS и SSL у вас должно работать:

1. **Telegram Bot:** ✅ Работает (telegram-bot контейнер)
2. **CalDAV календарь:** ✅ http://95.163.227.26:5232
3. **Веб-приложение:**
   - ⏳ http://95.163.227.26 (работает по IP)
   - ⏳ http://этонесамыйдлинныйдомен.рф (ждёт DNS)
   - ⏳ https://этонесамыйдлинныйдомен.рф (после SSL)

---

## 📝 Следующие шаги

1. ✅ Зайти в панель REG.RU
2. ✅ Добавить A-записи DNS
3. ⏰ Подождать 15-30 минут
4. ✅ Проверить домен в браузере
5. ✅ Установить SSL через certbot
6. ✅ Проверить HTTPS версию

**После выполнения всех шагов ваше приложение будет полностью готово!**

---

**Текущий статус:** Готово к настройке DNS ✅
**IP сервера:** 95.163.227.26
**Домен:** этонесамыйдлинныйдомен.рф
**Punycode:** xn--80aaagmglcejiocg0afgqeuc4azf.xn--p1ai

---
