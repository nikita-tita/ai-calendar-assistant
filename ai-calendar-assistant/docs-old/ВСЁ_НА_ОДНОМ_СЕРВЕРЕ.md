# 🎉 AI Calendar Bot - ВСЁ НА ОДНОМ СЕРВЕРЕ REG.RU!

## ✅ Что работает на REG.RU (91.229.8.221)

### 1. 🤖 Telegram Bot
- **Статус:** ✅ Запущен и работает
- **Функции:**
  - Распознавание голоса (Whisper через OpenAI API)
  - AI обработка (Claude от Anthropic)
  - Работа с календарём (CalDAV)
  - Ежедневные напоминания (9:00 и 20:00)
- **Токен:** 8378762774:AAE7oEvJX3fcHmLTQJPzQb9EIgQHXUWuaPI

### 2. 📅 Radicale Calendar Server
- **Статус:** ✅ Запущен НА ЭТОМ ЖЕ СЕРВЕРЕ
- **Адрес:** http://91.229.8.221:5232
- **Логин:** admin
- **Пароль:** admin123
- **Интеграция:** Бот подключён к этому календарю

### 3. 🖥️ Сервер
- **Хостинг:** REG.RU VPS
- **IP:** 91.229.8.221
- **ОС:** Ubuntu 22.04 LTS
- **Стоимость:** ~200₽/месяц
- **Использовано места:** 2.6GB из 9.8GB

---

## 🌟 Преимущества такой конфигурации

✅ **Всё в одном месте** - не нужно управлять несколькими серверами
✅ **Быстрая связь** - бот и календарь общаются напрямую без интернета
✅ **Дешевле** - один VPS вместо двух сервисов
✅ **Проще управлять** - все логи и настройки в одном месте
✅ **Надёжнее** - нет зависимости от внешних сервисов

---

## 📍 Как это организовано

```
┌─────────────────────────────────────────┐
│   REG.RU VPS (91.229.8.221)            │
│                                         │
│  ┌──────────────┐   ┌───────────────┐  │
│  │              │   │               │  │
│  │  Telegram    ├───┤   Radicale    │  │
│  │     Bot      │   │   Calendar    │  │
│  │              │   │               │  │
│  │  Container   │   │  Container    │  │
│  │  (Python)    │   │  (Port 5232)  │  │
│  └──────┬───────┘   └───────┬───────┘  │
│         │                   │          │
│         └───────┬───────────┘          │
│                 │                      │
│         Docker Network                 │
│     ai-calendar-assistant_default      │
└─────────────────────────────────────────┘
           │
           │ Интернет
           │
    ┌──────┴──────┐
    │  Telegram   │
    │    API      │
    └─────────────┘
```

**Важно:**
- Бот и календарь находятся в одной Docker-сети
- Бот обращается к календарю по имени: `http://radicale:5232`
- Календарь доступен снаружи по адресу: `http://91.229.8.221:5232`

---

## 🔗 Доступ к календарю

### Через браузер:
1. Откройте: http://91.229.8.221:5232
2. Введите:
   - Логин: `admin`
   - Пароль: `admin123`

### Из календарных приложений:

**Apple Calendar (iPhone/iPad/Mac):**
1. Настройки → Учётные записи
2. Добавить учётную запись → Другое
3. Добавить CalDAV-учётную запись:
   - **Сервер:** `91.229.8.221:5232`
   - **Имя пользователя:** `admin`
   - **Пароль:** `admin123`
   - **Описание:** AI Calendar Bot

**Google Calendar:**
1. Настройки → Добавить календарь
2. Из URL: `http://91.229.8.221:5232/admin/`

**Thunderbird:**
1. Календарь → Создать → В сети
2. CalDAV
3. URL: `http://91.229.8.221:5232/admin/`
4. Логин: `admin`, Пароль: `admin123`

---

## 🎯 Как пользоваться

### В Telegram:

1. Найдите своего бота
2. Отправьте `/start`
3. Создавайте события голосом или текстом:
   - "Встреча завтра в 15:00"
   - "Позвонить маме в пятницу вечером"
   - "Сходить к врачу 15 октября в 10:30"

### Напоминания:

Бот автоматически присылает напоминания:
- **Утром в 9:00** - события на сегодня
- **Вечером в 20:00** - события на завтра

---

## ⚙️ Управление

### Подключение к серверу:

```bash
ssh root@91.229.8.221
# Пароль: upvzrr3LH4pxsaqs
```

### Базовые команды:

```bash
cd /root/ai-calendar-assistant

# Статус обоих сервисов
docker ps

# Логи бота
docker logs -f telegram-bot

# Логи календаря
docker logs -f radicale

# Перезапуск бота
docker restart telegram-bot

# Перезапуск календаря
docker restart radicale

# Перезапуск всего
docker-compose -f docker-compose.full.yml restart

# Остановка всего
docker-compose -f docker-compose.full.yml down

# Запуск всего
docker-compose -f docker-compose.full.yml up -d
```

---

## 🔧 Настройки

### Файл .env (настройки бота):

```bash
nano /root/ai-calendar-assistant/.env
```

**Текущие настройки:**
```env
TELEGRAM_BOT_TOKEN=8378762774:AAE7oEvJX3fcHmLTQJPzQb9EIgQHXUWuaPI
OPENAI_API_KEY=your_openai_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here
RADICALE_URL=http://radicale:5232  ← Локальный календарь!
RADICALE_USERNAME=admin
RADICALE_PASSWORD=admin123
ENVIRONMENT=production
LOG_LEVEL=INFO
DEFAULT_TIMEZONE=Europe/Moscow
```

**После изменений:**
```bash
docker-compose -f docker-compose.full.yml restart
```

---

## 📊 Мониторинг

### Проверка статуса:

```bash
# На сервере
docker ps

# Должны видеть:
# telegram-bot   Up X minutes (healthy)
# radicale       Up X minutes (healthy)
```

### Проверка места:

```bash
df -h /
free -h
docker system df
```

### Проверка доступности:

```bash
# Бот
docker logs --tail 10 telegram-bot

# Календарь
curl http://localhost:5232
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Смените пароль календаря:**
   ```bash
   # Сгенерировать bcrypt hash
   htpasswd -bnBC 10 "" "новый_сложный_пароль" | tr -d ':\n'

   # Обновить
   nano /root/ai-calendar-assistant/radicale_config/users
   ```

2. **Настройте firewall:**
   ```bash
   ufw allow 22/tcp     # SSH
   ufw allow 5232/tcp   # Radicale
   ufw enable
   ```

3. **Получите домен и SSL (для production):**
   - Зарегистрируйте домен (например, в REG.RU)
   - Настройте Nginx как reverse proxy
   - Получите бесплатный SSL от Let's Encrypt

---

## 💾 Backup

### Создание backup:

```bash
ssh root@91.229.8.221

cd /root/ai-calendar-assistant

# Backup календаря
tar -czf backup_$(date +%Y%m%d).tar.gz radicale_data/ radicale_config/ .env

# Скачать на локальный компьютер
# На вашем Mac:
scp root@91.229.8.221:/root/ai-calendar-assistant/backup_*.tar.gz ~/Desktop/
```

### Восстановление:

```bash
cd /root/ai-calendar-assistant
docker-compose -f docker-compose.full.yml down
tar -xzf backup_YYYYMMDD.tar.gz
docker-compose -f docker-compose.full.yml up -d
```

---

## 🆘 Решение проблем

### Календарь не открывается:

```bash
# Проверить статус
docker ps | grep radicale

# Логи
docker logs radicale

# Проверить порт
curl http://localhost:5232

# Перезапустить
docker restart radicale
```

### Бот не создаёт события:

```bash
# Проверить подключение к календарю
docker exec telegram-bot env | grep RADICALE

# Должно быть:
# RADICALE_URL=http://radicale:5232

# Проверить логи бота
docker logs telegram-bot | grep -i calendar
docker logs telegram-bot | grep -i error
```

### Нет места на диске:

```bash
# Очистить Docker
docker system prune -a

# Удалить старые логи
rm -rf /root/ai-calendar-assistant/logs/*

# Проверить место
df -h
```

---

## 📈 Апгрейд сервера (если нужно)

Если не хватает ресурсов:

1. Зайдите в личный кабинет REG.RU
2. Ваш VPS → Изменить тариф
3. Увеличьте:
   - RAM до 1GB (+100₽/мес)
   - Диск до 20GB (+50₽/мес)

---

## 📋 Быстрая справка

```bash
# Подключение
ssh root@91.229.8.221
# Пароль: upvzrr3LH4pxsaqs

# Статус
cd /root/ai-calendar-assistant && docker ps

# Логи
docker logs -f telegram-bot    # Бот
docker logs -f radicale         # Календарь

# Перезапуск
docker restart telegram-bot     # Только бот
docker restart radicale         # Только календарь
docker-compose -f docker-compose.full.yml restart  # Всё

# Остановка
docker-compose -f docker-compose.full.yml down

# Запуск
docker-compose -f docker-compose.full.yml up -d
```

---

## ✅ Итоговый чеклист

- [x] Бот запущен на REG.RU ✅
- [x] Календарь запущен на REG.RU ✅
- [x] Бот подключён к календарю ✅
- [x] Календарь доступен извне ✅
- [x] Нет конфликтов токенов ✅
- [x] Всё работает автономно ✅

---

## 💰 Стоимость

**REG.RU VPS: ~200₽/месяц**

Включает:
- ✅ Telegram бот 24/7
- ✅ CalDAV календарь
- ✅ Автоматические напоминания
- ✅ Распознавание голоса
- ✅ AI обработка текста

**VS Railway (старая схема):**
- Railway bot: $5/месяц (~500₽)
- Railway Radicale: $5/месяц (~500₽)
- **Итого экономия: ~800₽/месяц!**

---

## 🎉 Заключение

**Всё работает на одном VPS REG.RU!**

- 🤖 Бот ловит сообщения из Telegram
- 📅 События сохраняются в локальный Radicale
- 🔔 Напоминания приходят вовремя
- 💾 Все данные на вашем сервере
- 💰 Стоит всего 200₽/месяц

**Никакого Railway - всё локально!** 🚀

---

**Документация создана:** 2025-10-10
**Сервер:** REG.RU VPS (91.229.8.221)
**Статус:** Production Ready ✅
