# 🚀 Как развернуть AI Calendar Bot на REG.RU

## Ваш сервер
- **IP:** 95.163.227.26
- **Логин:** root
- **Пароль:** xZV5uNNlvqd9G01r

---

## ⚡ БЫСТРЫЙ СПОСОБ (5-10 минут)

### Шаг 1: Откройте веб-консоль REG.RU

1. Перейдите на https://www.reg.ru/user/account
2. Зайдите в **"Серверы"** → **"VPS"**
3. Найдите сервер **"Sapphire Palladium"**
4. Нажмите **"Консоль"** или **"VNC"**
5. Войдите: логин `root`, пароль `xZV5uNNlvqd9G01r`

### Шаг 2: Загрузите файлы на сервер

У вас на рабочем столе есть 2 файла:
- `ai-calendar-bot-deploy.tar.gz` (архив с кодом)
- `install-from-archive.sh` (скрипт установки)

**Вариант A: Через веб-интерфейс REG.RU** (если есть файл-менеджер)

1. В панели управления VPS найдите раздел "Файлы" или "File Manager"
2. Загрузите оба файла в /root/

**Вариант B: Через команду wget** (рекомендуется)

Сначала загрузите файлы на любой доступный сервер (например, Dropbox, Google Drive, или свой сайт), затем в консоли сервера:

```bash
cd /root
wget https://ваша-ссылка-на-файл/ai-calendar-bot-deploy.tar.gz
wget https://ваша-ссылка-на-файл/install-from-archive.sh
```

**Вариант C: Создать всё вручную** (самый надёжный)

В веб-консоли выполните по порядку:

```bash
# 1. Установка необходимых пакетов
apt-get update && apt-get install -y curl git docker.io docker-compose

# 2. Запуск Docker
systemctl enable docker
systemctl start docker

# 3. Создание директории
mkdir -p /root/ai-calendar-assistant/app
cd /root/ai-calendar-assistant
```

Теперь создайте файлы (копируйте по одному в консоль):

**Создайте .env:**
```bash
cat > .env << 'EOF'
TELEGRAM_BOT_TOKEN=8378762774:AAEYXYVmx84pgp2_sn_U7RXYTgfNJMY-Klk
OPENAI_API_KEY=ваш_ключ_openai
ANTHROPIC_API_KEY=ваш_ключ_anthropic
RADICALE_URL=https://calendar-bot-production-e1ac.up.railway.app
ENVIRONMENT=production
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8000
DEFAULT_TIMEZONE=Europe/Moscow
EOF
```

**Создайте Dockerfile.bot:**
```bash
cat > Dockerfile.bot << 'EOF'
FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y ffmpeg && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "run_polling.py"]
EOF
```

**Создайте requirements.txt:**
```bash
cat > requirements.txt << 'EOF'
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-telegram-bot>=21.0
caldav>=1.3.0
icalendar>=5.0.0
anthropic>=0.8.0
python-dateutil==2.8.2
dateparser==1.2.0
pytz==2023.3
openai-whisper==20231117
openai>=1.50.0
pydantic==2.5.2
pydantic-settings==2.1.0
python-dotenv==1.0.0
httpx>=0.25.0
aiohttp>=3.9.0
tenacity==8.2.3
structlog==23.2.0
EOF
```

**Создайте docker-compose.production.yml:**
```bash
cat > docker-compose.production.yml << 'EOF'
version: '3.8'

services:
  telegram-bot:
    container_name: telegram-bot
    build:
      context: .
      dockerfile: Dockerfile.bot
    env_file:
      - .env
    restart: always
    volumes:
      - ./logs:/app/logs
      - ./credentials:/app/credentials
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
EOF
```

**Создайте run_polling.py:**
```bash
cat > run_polling.py << 'EOFPYTHON'
#!/usr/bin/env python3
import os
import logging
from telegram import Update
from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes

logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)
logger = logging.getLogger(__name__)

TELEGRAM_BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        '🤖 AI Calendar Assistant запущен!\n\n'
        'Команды:\n/start - Начало\n/help - Помощь'
    )

async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        '📋 Помощь:\n\nОтправьте событие, и я добавлю в календарь.\n'
        'Пример: "Встреча завтра в 15:00"'
    )

async def echo(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(f"Получено: {update.message.text}")

def main():
    if not TELEGRAM_BOT_TOKEN:
        logger.error("TELEGRAM_BOT_TOKEN не установлен!")
        return

    logger.info("🚀 Запуск бота...")
    application = Application.builder().token(TELEGRAM_BOT_TOKEN).build()
    application.add_handler(CommandHandler("start", start))
    application.add_handler(CommandHandler("help", help_command))
    application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, echo))
    logger.info("✅ Бот запущен")
    application.run_polling(allowed_updates=Update.ALL_TYPES)

if __name__ == '__main__':
    main()
EOFPYTHON
```

**Создайте минимальный app/__init__.py:**
```bash
mkdir -p app
touch app/__init__.py
```

### Шаг 3: Запустите бота

```bash
cd /root/ai-calendar-assistant

# Создайте директории
mkdir -p logs credentials

# Запустите
docker-compose -f docker-compose.production.yml up -d --build
```

Это займёт 2-5 минут (загрузка образов, установка зависимостей).

### Шаг 4: Проверьте работу

```bash
# Проверка статуса
docker ps

# Должны увидеть:
# CONTAINER ID   IMAGE                  COMMAND                  STATUS
# xxxxx          telegram-bot           "python run_polling.py"  Up X seconds

# Посмотрите логи
docker logs -f telegram-bot

# Должны увидеть:
# ✅ Бот запущен
# INFO - Application started
```

Нажмите `Ctrl+C` чтобы выйти из логов.

### Шаг 5: Проверьте в Telegram

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Вы должны получить ответ!

---

## 🔧 Полезные команды

### В веб-консоли сервера:

```bash
# Логи бота
docker logs -f telegram-bot

# Последние 100 строк логов
docker logs --tail 100 telegram-bot

# Перезапуск бота
docker restart telegram-bot

# Остановка
docker stop telegram-bot

# Запуск
docker start telegram-bot

# Статус
docker ps

# Использование ресурсов
docker stats telegram-bot

# Полный перезапуск с пересборкой
cd /root/ai-calendar-assistant
docker-compose -f docker-compose.production.yml down
docker-compose -f docker-compose.production.yml up -d --build
```

---

## 🐛 Решение проблем

### Бот не отвечает в Telegram

```bash
# Проверьте логи на ошибки
docker logs telegram-bot

# Проверьте что контейнер запущен
docker ps | grep telegram-bot

# Проверьте .env файл
cat /root/ai-calendar-assistant/.env
```

### Ошибка "TELEGRAM_BOT_TOKEN не установлен"

```bash
# Откройте .env
cd /root/ai-calendar-assistant
nano .env

# Убедитесь что строка выглядит так (БЕЗ пробелов):
# TELEGRAM_BOT_TOKEN=8378762774:AAEYXYVmx84pgp2_sn_U7RXYTgfNJMY-Klk

# Сохраните: Ctrl+X → Y → Enter

# Перезапустите
docker-compose -f docker-compose.production.yml restart
```

### Нет места на диске

```bash
# Очистка Docker
docker system prune -a

# Удаление логов
rm -rf /root/ai-calendar-assistant/logs/*
```

### Нехватка памяти

```bash
# Создайте swap
fallocate -l 1G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab

# Проверьте
free -h
```

---

## 📞 Поддержка REG.RU

- **Телефон:** 8 800 333-33-33
- **Email:** support@reg.ru
- **Личный кабинет:** https://www.reg.ru/user/account

---

## ✅ Чеклист

- [ ] Открыл веб-консоль REG.RU
- [ ] Подключился к серверу (root / xZV5uNNlvqd9G01r)
- [ ] Установил Docker и Docker Compose
- [ ] Создал все файлы проекта
- [ ] Заполнил .env с правильными токенами
- [ ] Запустил `docker-compose up -d --build`
- [ ] `docker ps` показывает telegram-bot
- [ ] `docker logs telegram-bot` без ошибок
- [ ] Бот отвечает в Telegram на `/start`

**Готово! Бот работает 24/7! 🎉**

---

## 💰 Стоимость

REG.RU VPS ~200₽/месяц

---

## 🔄 Обновление бота

Если нужно обновить код:

```bash
# Остановите старую версию
docker-compose -f docker-compose.production.yml down

# Замените файлы (загрузите новый архив и распакуйте)

# Запустите снова
docker-compose -f docker-compose.production.yml up -d --build
```

---

**Вопросы? Смотрите подробную инструкцию в MANUAL_DEPLOY_STEPS.md**
