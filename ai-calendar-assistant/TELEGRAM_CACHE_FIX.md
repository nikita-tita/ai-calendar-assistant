# 🔧 Исправление кеша Telegram Web App

**Проблема:** В боте показывает 30 октября вместо текущей даты
**Причина:** Telegram агрессивно кеширует Web App
**Решение:** Принудительная очистка кеша

---

## 🎯 Метод 1: Полная очистка кеша (Рекомендую)

### Desktop Telegram (Mac/Windows):

1. **Закройте Telegram полностью**
   - Mac: Cmd + Q
   - Windows: Правая кнопка на трее → Exit

2. **Очистите кеш:**
   - Mac:
     ```bash
     rm -rf ~/Library/Application\ Support/Telegram\ Desktop/tdata/user_data/cache
     rm -rf ~/Library/Application\ Support/Telegram\ Desktop/tdata/user_data/media_cache
     ```
   - Windows:
     ```
     Удалите папки:
     %APPDATA%\Telegram Desktop\tdata\user_data\cache
     %APPDATA%\Telegram Desktop\tdata\user_data\media_cache
     ```

3. **Откройте Telegram заново**

4. **Откройте бота и нажмите кнопку "📅 Календарь"**

---

## 🎯 Метод 2: Через настройки Telegram

### Desktop:

1. Откройте Telegram
2. Settings (⚙️)
3. Advanced
4. Manage local storage → Clear cache
5. Перезапустите Telegram
6. Откройте бота

### Mobile (iOS/Android):

1. Settings → Data and Storage → Storage Usage
2. Clear Cache
3. Закройте приложение полностью (смахните вверх)
4. Откройте заново
5. Откройте бота

---

## 🎯 Метод 3: Используйте Web Telegram

**Самый надёжный способ!**

1. Откройте: https://web.telegram.org/a/
2. Войдите в аккаунт
3. Откройте бота: [@aibroker_bot](https://t.me/aibroker_bot)
4. Нажмите кнопку "📅 Календарь"
5. Если всё ещё старая дата:
   - Откройте DevTools (F12)
   - Вкладка Application → Storage → Clear site data
   - Перезагрузите страницу (Ctrl+Shift+R)

---

## 🎯 Метод 4: Прямая ссылка (Обход кеша)

Откройте напрямую с параметром версии:

```
https://calendar.housler.ru/?v=202511242321&tgWebAppStartParam=bypass
```

Это гарантированно загрузит свежую версию.

---

## 🎯 Метод 5: Для разработки

Если нужно часто обновлять, добавьте в код бота timestamp:

```python
# В bot.py добавьте версию к URL
import time
version = int(time.time())
webapp_url = f"https://calendar.housler.ru/?v={version}"
```

---

## 🧪 Как проверить что загрузилась новая версия:

### 1. Откройте Web App
### 2. Откройте DevTools (правой кнопкой → Inspect)
### 3. В Console выполните:

```javascript
// Проверить дату
console.log('Selected Date:', new Date());

// Проверить размер файла
console.log('HTML size:', document.documentElement.outerHTML.length);
// Должно быть: 31934

// Проверить инициализацию
document.querySelector('script').textContent.includes('let selectedDate = new Date()');
// Должно быть: true
```

---

## ✅ Что должно показываться:

### После успешной очистки кеша:

- ✅ **Дата:** 24 ноября 2025 (или текущая)
- ✅ **Вкладка "✅ Задачи":** Показывает TODO
- ✅ **Календарь:** Можно открыть и выбрать дату

---

## 🔍 Если ничего не помогло:

### Проверьте что сервер отдаёт правильный файл:

```bash
# В Terminal на Mac:
curl -s https://calendar.housler.ru/ | wc -c
# Должно быть: 31934

curl -s https://calendar.housler.ru/ | grep "new Date()"
# Должно найти строки с инициализацией даты
```

Если сервер отдаёт правильный файл (31934 bytes и есть `new Date()`), значит проблема точно в кеше Telegram.

---

## 📱 Специфика кеша Telegram:

Telegram кеширует Web App на нескольких уровнях:

1. **Локальный кеш приложения** (Метод 1-2)
2. **HTTP кеш браузерного движка** (Метод 3)
3. **Service Worker** (если есть)
4. **Telegram CDN** (иногда)

Поэтому нужно очистить **ВСЁ**.

---

## 🎯 Рекомендуемая последовательность:

1. **Попробуйте Web Telegram** (Метод 3) - быстро и надёжно
2. Если не помогло - **Очистите кеш приложения** (Метод 2)
3. Если всё ещё не работает - **Удалите папки кеша** (Метод 1)
4. В крайнем случае - **Откройте прямую ссылку** (Метод 4)

---

## 💡 На будущее:

### Чтобы избежать проблем с кешем:

1. **В production всегда используйте версионирование:**
   ```
   https://calendar.housler.ru/?v=20251124
   ```

2. **Обновляйте Menu Button при каждом деплое:**
   ```bash
   ./update_menu_button.sh
   ```

3. **Добавьте в код автоматическое обновление:**
   ```javascript
   // Проверять версию и перезагружать если устарела
   const currentVersion = '20251124';
   const storedVersion = localStorage.getItem('appVersion');
   if (storedVersion !== currentVersion) {
       localStorage.setItem('appVersion', currentVersion);
       window.location.reload(true);
   }
   ```

---

## 🚀 Быстрая команда для обновления:

```bash
# Обновить Menu Button с новой версией
./update_menu_button.sh

# Затем в Telegram:
# 1. Закрыть приложение
# 2. Открыть заново
# 3. Открыть бота
```

---

## ✅ Итог:

**Проблема:** Telegram кеширует старую версию Web App
**Решение:** Очистить кеш одним из методов выше
**Проверка:** Дата должна быть текущая (24 ноября 2025)

---

**После очистки кеша всё заработает!** 🎉
