# 📝 Блог с автоматическим парсингом и рерайтингом

Система для автоматического парсинга статей из внешних источников (например, Cian.ru Magazine), рерайтинга через Yandex GPT и публикации в блоге.

## Возможности

- 📰 **Парсинг статей** из различных источников (Cian Magazine, и другие)
- 🤖 **Автоматический рерайтинг** через Yandex GPT
- 📝 **Сохранение структуры** - текст разбивается на параграфы с заголовками
- 🖼️ **Обработка изображений** - автоматическое скачивание главного изображения
- 🏷️ **SEO оптимизация** - автогенерация meta-описаний и заголовков
- 📚 **Категории и теги** для организации контента
- 🔄 **REST API** для управления блогом
- 💻 **CLI инструмент** для парсинга и публикации

## Архитектура

```
app/
├── models/
│   └── blog.py              # SQLAlchemy модели (BlogArticle, BlogCategory, BlogSource)
├── schemas/
│   └── blog.py              # Pydantic схемы для API
├── services/
│   └── blog/
│       ├── parser.py        # Парсеры статей (CianParser, GenericParser)
│       ├── rewriter.py      # Рерайтинг через Yandex GPT
│       └── blog_service.py  # CRUD операции с блогом
├── routers/
│   └── blog.py              # FastAPI endpoints
└── main.py                  # Подключение blog router

blog_parser_cli.py           # CLI инструмент для парсинга
```

## Установка

### 1. Установите зависимости

```bash
cd ai-calendar-assistant
pip install -r requirements.txt
```

Новые зависимости для блога:
- `beautifulsoup4` - парсинг HTML
- `transliterate` - транслитерация для URL slug
- `html2text` - конвертация HTML в markdown

### 2. Настройте переменные окружения

В `.env` файле должны быть настроены:

```bash
# Yandex GPT (обязательно для рерайтинга)
YANDEX_GPT_API_KEY=your_api_key_here
YANDEX_GPT_FOLDER_ID=your_folder_id_here

# База данных (используется существующая)
DATABASE_URL=sqlite:///./calendar_assistant.db
```

### 3. Создайте таблицы базы данных

При первом запуске таблицы создадутся автоматически:

```bash
python blog_parser_cli.py stats
```

## Использование

### CLI (Command Line Interface)

#### Создать категорию

```bash
python blog_parser_cli.py create-category "Недвижимость" "nedvizhimost" \
  --description "Статьи о рынке недвижимости"
```

#### Спарсить одну статью

```bash
# С автоматическим рерайтом и публикацией
python blog_parser_cli.py parse "https://spb.cian.ru/magazine/article-url/" \
  --parser cian \
  --publish \
  --category-id 1

# Без рерайта (только парсинг)
python blog_parser_cli.py parse "https://spb.cian.ru/magazine/article-url/" \
  --no-rewrite
```

#### Пакетный парсинг статей

```bash
# Спарсить 10 статей с главной страницы Cian Magazine
python blog_parser_cli.py batch "https://spb.cian.ru/magazine/" \
  --limit 10 \
  --parser cian \
  --category-id 1

# С автоматической публикацией
python blog_parser_cli.py batch "https://spb.cian.ru/magazine/" \
  --limit 15 \
  --publish \
  --category-id 1
```

#### Список статей

```bash
# Показать опубликованные статьи
python blog_parser_cli.py list --limit 20

# Показать все статьи (включая черновики)
python blog_parser_cli.py list --all
```

#### Управление публикацией

```bash
# Опубликовать статью
python blog_parser_cli.py publish 5

# Снять с публикации
python blog_parser_cli.py unpublish 5
```

#### Статистика

```bash
python blog_parser_cli.py stats
```

Вывод:
```
📊 Статистика блога:
   Всего статей: 25
   Опубликовано: 15
   Черновики: 10
   Категорий: 3
```

### REST API

Запустите FastAPI приложение:

```bash
cd ai-calendar-assistant
python app/main.py
```

API будет доступен на `http://localhost:8000`

Документация API: `http://localhost:8000/docs`

#### Основные endpoints:

**Категории:**
- `POST /api/blog/categories` - создать категорию
- `GET /api/blog/categories` - список категорий
- `GET /api/blog/categories/{id}` - получить категорию

**Статьи:**
- `POST /api/blog/articles` - создать статью
- `GET /api/blog/articles` - список статей (с пагинацией)
- `GET /api/blog/articles/{id}` - получить статью
- `GET /api/blog/articles/slug/{slug}` - получить статью по slug
- `PATCH /api/blog/articles/{id}` - обновить статью
- `DELETE /api/blog/articles/{id}` - удалить статью
- `POST /api/blog/articles/{id}/publish` - опубликовать
- `POST /api/blog/articles/{id}/unpublish` - снять с публикации

**Парсинг:**
- `POST /api/blog/parse/article?url=...` - спарсить одну статью
- `POST /api/blog/parse/batch?source_url=...` - пакетный парсинг

**Рерайтинг:**
- `POST /api/blog/rewrite` - рерайт текста через Yandex GPT

**Статистика:**
- `GET /api/blog/statistics` - статистика блога

#### Примеры запросов:

**Спарсить статью через API:**

```bash
curl -X POST "http://localhost:8000/api/blog/parse/article" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://spb.cian.ru/magazine/article-url/",
    "parser_type": "cian",
    "auto_rewrite": true,
    "auto_publish": false,
    "category_id": 1
  }'
```

**Получить список статей:**

```bash
curl "http://localhost:8000/api/blog/articles?skip=0&limit=20&published_only=true"
```

**Рерайт текста:**

```bash
curl -X POST "http://localhost:8000/api/blog/rewrite" \
  -H "Content-Type: application/json" \
  -d '{
    "original_content": "Оригинальный текст статьи...",
    "title": "Заголовок статьи",
    "preserve_structure": true
  }'
```

## Как работает рерайтинг

Yandex GPT используется для:

1. **Рерайт основного текста:**
   - Перепись своими словами для уникальности
   - Сохранение структуры с заголовками
   - Разбивка на параграфы (не один сплошной текст!)
   - Markdown разметка

2. **Генерация метаданных:**
   - Краткое описание (summary) 2-3 предложения
   - SEO meta description (до 160 символов)
   - Улучшенный заголовок

## Структура данных

### BlogArticle

```python
{
    "id": 1,
    "title": "Заголовок статьи",
    "slug": "zagolovok-stati",
    "original_content": "Оригинальный текст...",
    "rewritten_content": "Переписанный текст в markdown...",
    "summary": "Краткое описание",
    "category_id": 1,
    "tags": "недвижимость,аренда,рынок",
    "main_image_url": "https://...",
    "main_image_path": "static/blog/images/...",
    "source_url": "https://spb.cian.ru/magazine/...",
    "source_name": "cian",
    "is_published": true,
    "published_at": "2025-11-23T10:00:00",
    "meta_title": "SEO заголовок",
    "meta_description": "SEO описание",
    "created_at": "2025-11-23T09:00:00",
    "updated_at": "2025-11-23T10:00:00"
}
```

### BlogCategory

```python
{
    "id": 1,
    "name": "Недвижимость",
    "slug": "nedvizhimost",
    "description": "Статьи о рынке недвижимости",
    "created_at": "2025-11-23T09:00:00"
}
```

## Добавление новых источников

Для добавления нового источника статей создайте новый парсер в `app/services/blog/parser.py`:

```python
class NewSourceParser(BaseParser):
    async def parse_article_list(self, url: str, limit: int = 10) -> List[str]:
        # Реализуйте парсинг списка статей
        pass

    async def parse_article(self, url: str) -> ParsedArticle:
        # Реализуйте парсинг отдельной статьи
        pass
```

И зарегистрируйте его в `BlogService`:

```python
self.parsers = {
    'cian': CianParser(),
    'new_source': NewSourceParser(),
}
```

## Расписание автоматического парсинга

Для регулярного автоматического парсинга используйте cron:

```bash
# Каждый день в 10:00 парсим 10 новых статей с Cian
0 10 * * * cd /path/to/ai-calendar-assistant && python blog_parser_cli.py batch "https://spb.cian.ru/magazine/" --limit 10 --category-id 1
```

## Troubleshooting

### Ошибка 403 при парсинге

Некоторые сайты блокируют запросы от ботов. Парсер уже использует правильные User-Agent заголовки, но если проблема сохраняется:

1. Проверьте доступность сайта в браузере
2. Используйте VPN если сайт блокирует ваш IP
3. Попробуйте добавить задержку между запросами

### Ошибки Yandex GPT API

Проверьте:
- `YANDEX_GPT_API_KEY` корректен
- `YANDEX_GPT_FOLDER_ID` корректен
- Достаточно квоты в Yandex Cloud
- Интернет-соединение работает

### Текст рерайта одним абзацем

Обновите промпт в `app/services/blog/rewriter.py` для более четкого указания разбивки на параграфы.

## Roadmap

- [ ] Интеграция с другими источниками (10-15 источников как планировалось)
- [ ] Генерация изображений через AI (Yandex Vision или другие)
- [ ] Автоматическое добавление релевантных изображений в текст
- [ ] Планировщик публикаций (отложенная публикация)
- [ ] Webhook для уведомлений о новых статьях
- [ ] RSS лента блога
- [ ] Поддержка мультиязычности статей

## Лицензия

MIT License - часть проекта AI Calendar Assistant
