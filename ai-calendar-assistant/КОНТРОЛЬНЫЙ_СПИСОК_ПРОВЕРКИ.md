# 🧪 Контрольный список проверки после архивации

**Дата:** 2025-11-01
**Статус:** Для проверки пользователем

---

## ✅ Обязательные проверки

### 1. Запуск приложения
```bash
cd ai-calendar-assistant
uvicorn app.main:app --reload
```
- [ ] Приложение запускается без ошибок
- [ ] Нет ошибок импорта Property модулей
- [ ] В логах нет упоминаний Property errors

### 2. Проверка Telegram бота

#### Команды:
- [ ] `/start` - показывает приветствие с согласиями
- [ ] `/calendar` - показывает сообщение о календаре
- [ ] `/settings` - открывает настройки
- [ ] `/timezone` - показывает выбор часового пояса
- [ ] `/property` - **НЕ** работает (ожидаемо)

#### Кнопки:
- [ ] "📋 Дела на сегодня" - работает
- [ ] "📅 Дела на завтра" - работает
- [ ] "📆 Дела на неделю" - работает
- [ ] "⚙️ Настройки" - работает
- [ ] "🛠 Сервисы" - работает и показывает 4 кнопки (БЕЗ Property)

#### Основной функционал:
- [ ] Создание события текстом: "Встреча завтра в 14:00"
- [ ] Голосовая команда работает
- [ ] Просмотр списка событий: "Что у меня на завтра?"
- [ ] Обновление события работает
- [ ] Удаление события работает

### 3. Проверка API endpoints

#### Работающие:
```bash
curl http://localhost:8000/health
curl http://localhost:8000/telegram/status
curl http://localhost:8000/healthcheck
```
- [ ] `/health` - возвращает status: ok
- [ ] `/telegram/status` - возвращает bot info
- [ ] `/healthcheck` - возвращает calendar status

#### НЕ работающие (ожидаемо):
```bash
curl http://localhost:8000/api/property/search  # Should return 404
curl http://localhost:8000/healthcheck/property  # Should return 404
```
- [ ] `/api/property/*` - возвращает 404 (ожидаемо)
- [ ] `/healthcheck/property` - возвращает 404 (ожидаемо)

### 4. Проверка логов

```bash
# Посмотреть логи при старте
uvicorn app.main:app --reload 2>&1 | grep -i "property\|error"
```
- [ ] Нет ошибок импорта Property модулей
- [ ] Нет warnings о Property feed scheduler
- [ ] Есть сообщение "application_started"

---

## 🔍 Опциональные проверки

### 5. Docker запуск
```bash
docker-compose up --build
```
- [ ] Контейнеры запускаются
- [ ] Бот работает через Docker
- [ ] Radicale доступен

### 6. Проверка базы данных
- [ ] Calendar события сохраняются в Radicale
- [ ] Нет ошибок при работе с календарем
- [ ] События читаются корректно

### 7. Проверка архива
```bash
ls -la _archived/property_bot_microservice/
```
- [ ] Архив существует
- [ ] Содержит все файлы Property Bot
- [ ] README_ARCHIVED.md на месте

---

## 🐛 Что делать при проблемах

### Ошибка импорта Property модулей:
1. Проверить, что все импорты закомментированы в:
   - `app/main.py`
   - `app/services/telegram_handler.py`
   - `app/routers/health.py`

2. Проверить, что `PROPERTY_BOT_ENABLED = False` в `telegram_handler.py`

### Бот не реагирует на команды:
1. Проверить TELEGRAM_BOT_TOKEN в .env
2. Проверить webhook настройки
3. Проверить логи: `docker logs telegram-bot-polling`

### Ошибки при запуске:
1. Проверить все зависимости: `pip install -r requirements.txt`
2. Проверить Python версию: должен быть 3.11+
3. Проверить .env файл

---

## 📊 Критерии успешной архивации

### ✅ УСПЕХ, если:
- Приложение запускается без ошибок
- Calendar Bot работает полностью
- Нет упоминаний Property в активном коде
- Property endpoints возвращают 404
- Архив полный и задокументирован

### ❌ ПРОБЛЕМА, если:
- Ошибки импорта Property при старте
- Calendar функции не работают
- Property endpoints все еще доступны
- Кнопки Property все еще видны в боте

---

## 📝 Отчет о проверке

После прохождения всех проверок, заполните:

**Дата проверки:** __________

**Проверил:** __________

**Результат:** [ ] ✅ Все работает | [ ] ❌ Есть проблемы

**Комментарии:**
```
_______________________________________________________________
_______________________________________________________________
_______________________________________________________________
```

---

**Статус:** Готово к проверке
