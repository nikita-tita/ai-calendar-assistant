# 🚀 Деплой системы блога с дайджестами

Пошаговая инструкция по развертыванию системы автоматического парсинга, рерайтинга и рассылки статей.

## 📋 Предварительные требования

### Обязательные сервисы:
- ✅ **Python 3.11+**
- ✅ **PostgreSQL** или **SQLite** (для разработки)
- ✅ **Telegram Bot** (токен от @BotFather)
- ✅ **Yandex GPT API** (ключ и folder_id)
- ✅ **Домен с HTTPS** (для Telegram WebApp)

### Проверка .env файла:
```bash
# Обязательные переменные для блога
YANDEX_GPT_API_KEY=your_api_key_here
YANDEX_GPT_FOLDER_ID=your_folder_id_here
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_WEBAPP_URL=https://yourdomain.com  # Важно для WebApp!
DATABASE_URL=sqlite:///./calendar_assistant.db  # или PostgreSQL
```

## 🔧 Шаг 1: Установка зависимостей

```bash
cd ai-calendar-assistant
pip install -r requirements.txt
```

Новые зависимости для блога:
- `beautifulsoup4` - парсинг HTML
- `transliterate` - транслитерация для URL slug
- `html2text` - конвертация HTML в markdown
- `jinja2` - шаблонизатор для WebApp

## 🗄️ Шаг 2: Инициализация базы данных

Таблицы создадутся автоматически при первом запуске:
- `blog_articles` - статьи
- `blog_categories` - категории
- `blog_sources` - источники для парсинга
- `blog_digests` - история отправленных дайджестов

```bash
# Проверка создания таблиц
python blog_parser_cli.py stats
```

Вывод должен быть:
```
📊 Статистика блога:
   Всего статей: 0
   Опубликовано: 0
   Черновики: 0
   Категорий: 0
```

## 📁 Шаг 3: Создание структуры каталогов

```bash
# Создать каталоги для статических файлов
mkdir -p static/blog/images
mkdir -p templates

# Проверить, что шаблон на месте
ls -la templates/blog_article.html
```

## 📝 Шаг 4: Настройка категорий

```bash
# Создать основную категорию для недвижимости
python blog_parser_cli.py create-category \
  "Недвижимость" \
  "nedvizhimost" \
  --description "Статьи о рынке недвижимости"

# Создать дополнительные категории (опционально)
python blog_parser_cli.py create-category \
  "Аренда" \
  "arenda" \
  --description "Аренда квартир и домов"

python blog_parser_cli.py create-category \
  "Покупка" \
  "pokupka" \
  --description "Покупка недвижимости"

# Проверить созданные категории
python blog_parser_cli.py categories
```

## 🔄 Шаг 5: Первоначальная загрузка статей

### Парсинг статей с Cian Magazine:

```bash
# Спарсить 10 статей с рерайтом и публикацией
python blog_parser_cli.py batch "https://spb.cian.ru/magazine/" \
  --limit 10 \
  --parser cian \
  --category-id 1 \
  --publish

# Это займет время (примерно 1-2 минуты на статью из-за рерайтинга)
```

### Проверка результата:

```bash
# Посмотреть список статей
python blog_parser_cli.py list --limit 10

# Статистика
python blog_parser_cli.py stats
```

## 🚀 Шаг 6: Запуск приложения

### Локально (для тестирования):

```bash
cd ai-calendar-assistant
python app/main.py
```

Проверьте лог:
```
INFO     application_started environment=development
INFO     blog_digest_scheduler_started schedule=Daily at 14:00
```

### Docker (production):

```bash
# Пересобрать образ
docker-compose build calendar-assistant

# Запустить
docker-compose up -d calendar-assistant

# Проверить логи
docker-compose logs -f calendar-assistant | grep blog_digest
```

## 📱 Шаг 7: Настройка Telegram бота

### 7.1 Обновить команды бота

Отправьте @BotFather:
```
/setcommands
```

Затем добавьте команду:
```
start - Запустить бота
calendar - Открыть календарь
blog - Получить подборку статей
settings - Настройки
```

### 7.2 Протестировать вручную

В Telegram боте отправьте:
```
/blog
```

Должны прийти 3 последние статьи с кнопками "Читать подробнее".

### 7.3 Проверить WebApp

1. Нажмите "📖 Читать подробнее" на любой статье
2. Должно открыться WebApp с полным текстом статьи
3. Проверьте, что:
   - ✅ Отображается заголовок
   - ✅ Текст разбит на параграфы
   - ✅ Есть изображение (если было в статье)
   - ✅ Работает темная тема (если включена в Telegram)

## ⏰ Шаг 8: Проверка scheduler

### Проверка что scheduler запущен:

```bash
# В логах должна быть строка
grep "blog_digest_scheduler_started" <путь_к_логам>
```

### Тестирование расписания:

**Вариант 1: Изменить время для теста**

Отредактируйте `app/services/blog/digest_scheduler.py`:
```python
# Было:
CronTrigger(hour=14, minute=0)

# Для теста (через 2 минуты от текущего времени):
CronTrigger(hour=16, minute=32)  # Замените на текущее время + 2 мин
```

Перезапустите приложение и дождитесь отправки.

**Вариант 2: Ручной вызов (рекомендуется)**

Добавьте временный endpoint в `app/routers/blog.py`:
```python
@router.post("/digest/send-now", include_in_schema=False)
async def send_digest_now():
    """Manually trigger digest sending (for testing)."""
    from app.services.blog.digest_scheduler import digest_scheduler
    await digest_scheduler.send_digest_now()
    return {"status": "Digest sent"}
```

Затем вызовите:
```bash
curl -X POST http://localhost:8000/api/blog/digest/send-now
```

### Проверить историю дайджестов:

```bash
# В БД должна появиться запись
sqlite3 calendar_assistant.db "SELECT * FROM blog_digests;"
```

## 🔄 Шаг 9: Автоматизация парсинга

### Настроить cron для регулярного парсинга:

```bash
# Редактировать crontab
crontab -e
```

Добавить задачи:
```bash
# Парсить 5 новых статей каждый день в 10:00
0 10 * * * cd /path/to/ai-calendar-assistant && python blog_parser_cli.py batch "https://spb.cian.ru/magazine/" --limit 5 --publish --category-id 1 >> /var/log/blog_parser.log 2>&1

# Парсить 10 статей каждую неделю в воскресенье в 02:00
0 2 * * 0 cd /path/to/ai-calendar-assistant && python blog_parser_cli.py batch "https://spb.cian.ru/magazine/" --limit 10 --publish --category-id 1 >> /var/log/blog_parser.log 2>&1
```

## 📊 Шаг 10: Мониторинг

### Проверить API endpoints:

```bash
# Список статей
curl http://localhost:8000/api/blog/articles?limit=5

# Статистика
curl http://localhost:8000/api/blog/statistics

# Категории
curl http://localhost:8000/api/blog/categories
```

### Логи для отслеживания:

```bash
# Отправка дайджестов
grep "digest_" <путь_к_логам>

# Парсинг статей
grep "parsing_article" <путь_к_логам>

# Рерайтинг
grep "rewriting_article" <путь_к_логам>
```

## 🎯 Чеклист готовности к production

- [ ] ✅ Все переменные в .env настроены
- [ ] ✅ Yandex GPT API ключ валиден
- [ ] ✅ TELEGRAM_WEBAPP_URL указывает на HTTPS домен
- [ ] ✅ База данных создана и мигрирована
- [ ] ✅ Категории созданы
- [ ] ✅ Загружены первые 10-15 статей
- [ ] ✅ Протестирован `/blog` команда
- [ ] ✅ WebApp открывается и показывает статью
- [ ] ✅ Scheduler запущен (в логах)
- [ ] ✅ Cron настроен для регулярного парсинга
- [ ] ✅ Логи мониторятся

## 🐛 Troubleshooting

### Проблема: Дайджест не отправляется в 14:00

**Причина:** Scheduler не запустился

**Решение:**
```bash
# Проверить логи
grep "blog_digest_scheduler" <путь_к_логам>

# Должна быть строка: blog_digest_scheduler_started
# Если нет - проверить ошибки при запуске
```

### Проблема: WebApp не открывается

**Причина:** Неправильный TELEGRAM_WEBAPP_URL

**Решение:**
1. Проверить `.env`:
   ```bash
   TELEGRAM_WEBAPP_URL=https://yourdomain.com  # БЕЗ слэша в конце
   ```
2. URL должен быть HTTPS (не HTTP)
3. Домен должен быть доступен извне

### Проблема: Статьи не парсятся (403 ошибка)

**Причина:** Сайт блокирует запросы

**Решение:**
1. Использовать VPN
2. Добавить задержку между запросами
3. Попробовать другой источник

### Проблема: Рерайт не работает

**Причина:** Ошибка Yandex GPT API

**Решение:**
```bash
# Проверить ключ и folder_id
echo $YANDEX_GPT_API_KEY
echo $YANDEX_GPT_FOLDER_ID

# Проверить квоту в Yandex Cloud
# https://console.cloud.yandex.ru/
```

### Проблема: В дайджесте нет статей

**Причина:** Нет опубликованных статей за последние 48 часов

**Решение:**
```bash
# Опубликовать черновики
python blog_parser_cli.py publish <article_id>

# Или спарсить новые с автопубликацией
python blog_parser_cli.py batch "https://spb.cian.ru/magazine/" --limit 5 --publish --category-id 1
```

## 📈 Рекомендации по production

### Производительность:

1. **PostgreSQL вместо SQLite** для production
   ```bash
   DATABASE_URL=postgresql://user:password@localhost/calendar_db
   ```

2. **Кэширование** статей для WebApp (Redis)
3. **CDN** для статических изображений
4. **Rate limiting** для парсинга (не более 1 запроса в 3 секунды)

### Безопасность:

1. **Валидация Telegram WebApp** данных (уже реализована)
2. **HTTPS обязателен** для WebApp
3. **Секреты в .env** не коммитить в git
4. **Ротация логов** настроить

### Масштабирование:

1. **Несколько источников** - добавить парсеры для 10-15 источников
2. **Персонализация** - позволить пользователям выбирать категории
3. **Аналитика** - отслеживать открытия статей
4. **A/B тесты** - разное время отправки для разных групп

## 🎉 Готово!

После выполнения всех шагов система будет:
- ✅ Автоматически парсить статьи каждый день
- ✅ Рерайтить через Yandex GPT для уникальности
- ✅ Отправлять дайджест в 14:00 всем пользователям
- ✅ Показывать статьи в красивом WebApp

## 📞 Поддержка

При возникновении проблем:
1. Проверить логи приложения
2. Проверить [BLOG_README.md](BLOG_README.md) для детальной документации
3. Создать issue в репозитории

---

**Версия:** 1.0
**Дата:** 23 ноября 2025
**Автор:** Claude AI + Nikita
