# 🔑 Добавление SSH ключа на VPS

**Нужно добавить SSH ключ на VPS один раз, чтобы дальше всё работало автоматически.**

---

## Вариант 1: Автоматически (Рекомендую) ⭐

Выполните команду и введите пароль VPS **один раз**:

```bash
ssh-copy-id -i ~/.ssh/calendar_deploy.pub root@91.229.8.221
```

**Что произойдёт:**
1. Запросит пароль VPS
2. Скопирует SSH ключ на сервер
3. Дальше пароль не нужен!

---

## Вариант 2: Вручную

Если команда выше не работает:

### Шаг 1: Покажите публичный ключ

```bash
cat ~/.ssh/calendar_deploy.pub
```

**Скопируйте весь вывод** (начинается с `ssh-ed25519 ...`)

### Шаг 2: Подключитесь к VPS

```bash
ssh root@91.229.8.221
# Введите пароль VPS
```

### Шаг 3: Добавьте ключ

```bash
# На VPS выполните:
mkdir -p ~/.ssh
chmod 700 ~/.ssh
echo "ВАШИХ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
exit
```

---

## Проверка

После добавления ключа проверьте:

```bash
ssh -i ~/.ssh/calendar_deploy root@91.229.8.221 'echo "SSH работает!"'
```

**Если видите "SSH работает!"** - ключ добавлен! ✅

---

## Дальше

После успешного добавления SSH ключа запустите:

```bash
cd ~/Desktop/AI-Calendar-Project/ai-calendar-assistant
./auto_setup_calendar.sh
```

Это автоматически настроит весь домен за 5 минут!

---

## ❓ Не помните пароль VPS?

Есть несколько вариантов:

### 1. Проверьте почту
Пароль был отправлен при создании VPS от Reg.ru

### 2. Проверьте панель Reg.ru
1. Войдите: https://www.reg.ru/user/vps/
2. Найдите VPS с IP 91.229.8.221
3. Может быть опция "Показать пароль" или "Сбросить пароль"

### 3. Сброс пароля
В панели управления VPS должна быть опция "Сбросить пароль root"

---

## 🚀 Быстрая шпаргалка

```bash
# 1. Добавить SSH ключ (введите пароль один раз)
ssh-copy-id -i ~/.ssh/calendar_deploy.pub root@91.229.8.221

# 2. Проверить
ssh -i ~/.ssh/calendar_deploy root@91.229.8.221 'echo "OK"'

# 3. Запустить автоматическую настройку
cd ~/Desktop/AI-Calendar-Project/ai-calendar-assistant
./auto_setup_calendar.sh
```

**Время: 5-7 минут**
