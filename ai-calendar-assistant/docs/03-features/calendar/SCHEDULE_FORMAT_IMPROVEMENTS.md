# Schedule Format - Улучшения и Уточнение Года

## Что было сделано

### Дата: 27 октября 2025

## 1. Основная функциональность (первая итерация)

✅ **Автоматическое распознавание формата расписания**
- Regex-based парсинг множественных временных диапазонов
- Поддержка форматов `HH:MM-HH:MM Название` и `HH:MM Название`
- Минимум 3 события для активации
- Ключевые слова: тайминг, расписание, schedule, agenda

✅ **Извлечение даты из текста**
- Относительные даты: завтра, послезавтра, сегодня
- Русские месяцы: "на 23 октября"
- Английские месяцы: "on October 23"
- Явный формат: DD.MM.YYYY

✅ **Интеграция с batch_confirm**
- Использует существующий механизм подтверждения
- Показывает сводку перед созданием
- Все события создаются одним действием

## 2. Улучшение: Уточнение года (вторая итерация)

### Проблема
Исходная реализация **автоматически** выбирала следующий год, если дата уже прошла. Это могло приводить к ошибкам:
- Пользователь мог иметь в виду прошедшую дату (для архива/записей)
- Автоматический выбор следующего года - предположение, которое может быть неверным

### Решение
Добавлен механизм **явного уточнения** у пользователя.

### Изменения в коде

**Файл:** [app/services/llm_agent_yandex.py](app/services/llm_agent_yandex.py)

#### 1. Обнаружение прошедших дат (строки 214-258)
```python
# If date is in the past (more than 1 day ago), it's ambiguous
from datetime import timedelta
if candidate_date < (now.date() - timedelta(days=1)):
    # Return None to let LLM handle clarification
    logger.info("schedule_date_ambiguous_past",
               date=f"{day} {month_name}",
               current_year=year,
               next_year=year+1)
    return None  # Let LLM ask for year clarification
```

#### 2. Обработка ответов пользователя (строки 182-204)
```python
# Check if this is a response to year clarification question
year_override = None
if conversation_history and len(conversation_history) > 0:
    last_message = conversation_history[-1]
    if last_message.get('role') == 'assistant':
        clarify_text = last_message.get('content', '').lower()
        # Check if last message was asking about year
        if ('года' in clarify_text or 'year' in clarify_text):
            text_lower = user_text.lower()
            if '2025' in text_lower or '2026' in text_lower:
                year_override = int(year_match.group(1))
            elif 'следующ' in text_lower or 'next' in text_lower:
                year_override = now.year + 1
            elif 'этого' in text_lower or 'this' in text_lower:
                year_override = now.year
```

#### 3. Использование year_override (строки 239-243)
```python
# Use year_override if provided by user in response to clarification
if year_override:
    year = year_override
    target_date = datetime(year, month, day, tzinfo=tz).date()
    logger.info("using_year_override", year=year, date=target_date)
```

#### 4. Обновление системного промпта (строки 109-113)
```
25. DATE AMBIGUITY for schedule format:
    - If date like "23 октября" is in the PAST, ASK for clarification
    - Example: "Уточните, пожалуйста: расписание на 23 октября 2025 или 2026?"
    - Do NOT automatically assume next year - ALWAYS ask user to confirm
```

### Поддерживаемые ответы пользователя

**Явные годы:**
- `2025`, `2026`, `2027`
- `2025 года`, `2026 года`

**Относительные:**
- `следующего года`, `следующий год`, `next year` → +1 год
- `этого года`, `текущего года`, `this year`, `current year` → текущий год

### Поведение системы

| Ситуация | Действие |
|----------|----------|
| Дата в будущем (завтра, через неделю) | Создает события без вопросов |
| Дата в прошлом (>1 дня назад) | **Спрашивает** какой год |
| Дата с явным годом (23.10.2026) | Создает события без вопросов |
| Относительная дата (завтра, сегодня) | Создает события без вопросов |

### Преимущества

1. ✅ **Точность** - нет ошибочных предположений
2. ✅ **Контроль** - пользователь явно выбирает год
3. ✅ **Гибкость** - можно создавать события и в прошлом (для архива)
4. ✅ **Прозрачность** - система показывает оба варианта
5. ✅ **UX** - не блокирует очевидные случаи (будущие даты, явный год)

## Примеры использования

### Пример 1: Дата в прошлом - требуется уточнение

```
Пользователь: тайминг на 23 октября:
              12:45-13:00 Приезд
              13:00-13:30 Кофе-брейк
              13:30-15:00 Дискуссия

Бот: Уточните, пожалуйста: расписание на 23 октября 2025 года или 2026 года?

Пользователь: следующего года

Бот: 📅 Создать 3 события
     📍 23 октября 2026, 12:45
     [Подтвердить] [Отменить]
```

### Пример 2: Дата в будущем - без уточнения

```
Пользователь: тайминг на 5 ноября:
              09:00-10:00 Встреча
              10:00-11:00 Планирование

Бот: 📅 Создать 2 события
     📍 5 ноября 2025, 09:00
     [Подтвердить] [Отменить]
```

### Пример 3: Явный год - без уточнения

```
Пользователь: расписание на 15.11.2026:
              09:00-10:00 Встреча
              10:00-11:00 Планирование

Бот: 📅 Создать 2 события
     📍 15 ноября 2026, 09:00
     [Подтвердить] [Отменить]
```

## Технические детали

### Измененные функции

1. `_detect_schedule_format()` - добавлен параметр `conversation_history`
2. `extract_event()` - передача `conversation_history` в `_detect_schedule_format()`

### Логика определения неоднозначности

```python
# Дата считается "прошедшей" если она более 1 дня назад
if candidate_date < (now.date() - timedelta(days=1)):
    return None  # Trigger LLM clarification
```

**Почему -1 день?**
- Сегодня и вчера = OK (очевидно текущий год)
- Более 1 дня назад = неоднозначно (может быть следующий год)

### Обработка контекста разговора

Система проверяет:
1. Есть ли история разговора?
2. Было ли последнее сообщение от ассистента?
3. Содержит ли оно ключевые слова "года"/"year" и годы "2025"/"2026"?
4. Если да → извлекает выбор пользователя из ответа

## Развертывание

✅ Код обновлен на сервере
✅ Бот перезапущен
✅ Функциональность активна

## Документация

Созданы следующие документы:
1. [BATCH_SCHEDULE_FEATURE.md](BATCH_SCHEDULE_FEATURE.md) - полное описание функции (обновлено)
2. [YEAR_CLARIFICATION_GUIDE.md](YEAR_CLARIFICATION_GUIDE.md) - руководство по уточнению года
3. [QUICK_SCHEDULE_GUIDE.md](QUICK_SCHEDULE_GUIDE.md) - краткая инструкция для пользователей
4. [SCHEDULE_FORMAT_IMPROVEMENTS.md](SCHEDULE_FORMAT_IMPROVEMENTS.md) - этот файл

## Тестирование

### Тестовые сценарии

1. ✅ Дата в прошлом (23 октября) → должен спросить год
2. ✅ Дата в будущем (5 ноября) → создает без вопросов
3. ✅ Явный год (23.10.2026) → создает без вопросов
4. ✅ Относительная дата (завтра) → создает без вопросов
5. ✅ Ответ "следующего года" → использует 2026
6. ✅ Ответ "этого года" → использует 2025
7. ✅ Ответ "2026" → использует 2026

## Возможные улучшения

- [ ] Поддержка диапазона дат (23-25 октября)
- [ ] Умный выбор года на основе контекста (если сейчас декабрь, и пользователь говорит "январь", скорее всего следующий год)
- [ ] Запоминание предпочтений пользователя (если часто выбирает следующий год)
- [ ] Поддержка других языков для уточнения

---

**Версия:** 1.1
**Дата:** 27 октября 2025
**Статус:** Развернуто в production
