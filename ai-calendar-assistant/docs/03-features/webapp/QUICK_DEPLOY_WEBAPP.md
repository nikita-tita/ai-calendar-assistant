# 🚀 Quick WebApp Deploy

## Перед деплоем

1. Отредактируйте `webapp_current.html`
2. Обновите версию:
   ```javascript
   const APP_VERSION = '2025-10-21-13:30'; // Текущая дата и время
   ```

## Деплой

```bash
./deploy-webapp-safe.sh
```

## Откат (если что-то сломалось)

```bash
# Посмотреть бэкапы
sshpass -p 'upvzrr3LH4pxsaqs' ssh root@91.229.8.221 "ls -lht /var/www/calendar_backup/"

# Откатиться (замените timestamp на нужный)
sshpass -p 'upvzrr3LH4pxsaqs' ssh root@91.229.8.221 \
  "cp /var/www/calendar_backup/index.html.TIMESTAMP /var/www/calendar/index.html"
```

## Проверка

```bash
# Версия на сервере
sshpass -p 'upvzrr3LH4pxsaqs' ssh root@91.229.8.221 \
  "grep APP_VERSION /var/www/calendar/index.html"

# Заголовки кэша
curl -sI https://xn--80aiabdqfcqhgchaebm7bp0qg3a.xn--p1ai/ | grep -i cache
```

## Что сделано

✅ Исправлена конфигурация Nginx (убраны вложенные location блоки)
✅ Добавлены правильные заголовки Cache-Control для HTML
✅ Добавлено автоматическое обнаружение версии в WebApp
✅ Создан безопасный скрипт деплоя с бэкапами
✅ Пользователи не потеряют данные (данные в БД, а не в WebApp)

## Для пользователей

После деплоя пользователям нужно просто **закрыть и открыть WebApp** в Telegram.
При следующем открытии автоматически загрузится новая версия.
