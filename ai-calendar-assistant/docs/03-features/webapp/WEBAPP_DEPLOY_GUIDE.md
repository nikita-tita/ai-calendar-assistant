# 📱 WebApp Deployment Guide

## 🔍 Обнаруженные проблемы

### 1. **Проблема с кэшированием в Nginx**
- Текущая конфигурация использовала **вложенные location блоки**, которые не работают в Nginx
- HTML файлы кэшировались, и пользователи видели старые версии

### 2. **Агрессивное кэширование в Telegram WebApp**
- Telegram WebApp очень агрессивно кэширует страницы
- Даже с правильными заголовками Cache-Control пользователи могут видеть старую версию

## ✅ Решение

### 1. **Исправлена конфигурация Nginx**
- Убраны вложенные location блоки
- Добавлены правильные заголовки для отключения кэша HTML
- Добавлено кэширование статических ресурсов (JS, CSS, изображения)

### 2. **Версионирование в WebApp**
- Добавлена автоматическая проверка версии при загрузке
- При обнаружении новой версии происходит автоматическая перезагрузка
- Используется localStorage для отслеживания версии

### 3. **Безопасный скрипт деплоя**
- Автоматическое создание бэкапов
- Атомарная замена файла (нет частичных обновлений)
- Автоматическая проверка после деплоя
- Автоматический откат при ошибках

## 🚀 Как делать деплой

### Быстрый деплой (рекомендуется)

```bash
./deploy-webapp-safe.sh
```

Этот скрипт:
1. ✅ Создаст бэкап текущей версии
2. ✅ Загрузит новую версию
3. ✅ Обновит конфигурацию Nginx (при необходимости)
4. ✅ Проверит успешность деплоя
5. ✅ Откатится автоматически при ошибке

### Ручной деплой

Если нужно сделать вручную:

```bash
# 1. Создать бэкап
sshpass -p '$SERVER_PASSWORD' ssh root@95.163.227.26 \
  "cp /var/www/calendar/index.html /var/www/calendar_backup/index.html.$(date +%Y%m%d_%H%M%S)"

# 2. Загрузить новый файл
sshpass -p '$SERVER_PASSWORD' scp webapp_current.html \
  root@95.163.227.26:/var/www/calendar/index.html

# 3. Проверить
sshpass -p '$SERVER_PASSWORD' ssh root@95.163.227.26 \
  "head -100 /var/www/calendar/index.html | grep Version"
```

## 🔄 Откат к предыдущей версии

Если что-то пошло не так:

```bash
# Посмотреть доступные бэкапы
sshpass -p '$SERVER_PASSWORD' ssh root@95.163.227.26 \
  "ls -lht /var/www/calendar_backup/"

# Откатиться к конкретному бэкапу
sshpass -p '$SERVER_PASSWORD' ssh root@95.163.227.26 \
  "cp /var/www/calendar_backup/index.html.20251021_120000 /var/www/calendar/index.html"
```

## 📝 Как обновить версию перед деплоем

В файле `webapp_current.html` найдите и обновите:

```javascript
const APP_VERSION = '2025-10-21-12:00';  // Измените на текущую дату/время
```

Также обновите комментарий:
```javascript
// Version: 2025-10-21-12:00 - Краткое описание изменений
```

## 🎯 Почему пользователи ничего не потеряют

### 1. **Атомарная замена файла**
- Используется `mv -f` для мгновенной замены
- Нет периода, когда файл недоступен или частично записан

### 2. **Данные хранятся на бэкенде**
- Все события пользователей хранятся в PostgreSQL
- WebApp - это только интерфейс, он не хранит данные
- При обновлении интерфейса данные остаются нетронутыми

### 3. **Автоматическая перезагрузка**
- При открытии обновленного WebApp происходит проверка версии
- Если версия изменилась, пользователь автоматически получит свежую версию
- localStorage очищается только для версионной информации, не для данных

### 4. **Бэкапы**
- Каждый деплой создает бэкап
- Можно быстро откатиться к любой из последних 10 версий
- Бэкапы хранятся в отдельной директории

## ⚠️ Важные замечания

### Для пользователей Telegram WebApp

После деплоя пользователям может потребоваться:
1. **Закрыть и заново открыть WebApp** (рекомендуется)
2. Или дождаться автоматической перезагрузки при следующем открытии

### Проверка после деплоя

```bash
# Проверить версию на сервере
sshpass -p '$SERVER_PASSWORD' ssh root@95.163.227.26 \
  "grep 'APP_VERSION' /var/www/calendar/index.html"

# Проверить заголовки cache-control
curl -I https://этонесамыйдлинныйдомен.рф | grep -i cache
```

## 🔧 Конфигурация Nginx

Текущая оптимальная конфигурация:

- **HTML файлы**: Cache-Control = no-store (без кэша)
- **Статические файлы** (JS, CSS, изображения): Cache-Control = public, 1 час
- **API запросы**: Без кэша, с поддержкой WebSocket upgrades

## 📊 Мониторинг

### Проверить логи Nginx
```bash
sshpass -p '$SERVER_PASSWORD' ssh root@95.163.227.26 \
  "tail -f /var/log/nginx/access.log"
```

### Проверить логи ошибок
```bash
sshpass -p '$SERVER_PASSWORD' ssh root@95.163.227.26 \
  "tail -f /var/log/nginx/error.log"
```

## 🎓 Best Practices

1. **Всегда обновляйте версию** перед деплоем
2. **Используйте скрипт деплоя** вместо ручного копирования
3. **Проверяйте работу** после каждого деплоя
4. **Тестируйте локально** перед деплоем на прод
5. **Документируйте изменения** в комментариях к версии

## 🆘 Troubleshooting

### Проблема: Пользователи видят старую версию

**Решение:**
1. Проверьте версию на сервере
2. Попросите пользователей закрыть и открыть WebApp заново
3. Проверьте заголовки cache-control в Nginx

### Проблема: Деплой завершился с ошибкой

**Решение:**
1. Проверьте логи: `./deploy-webapp-safe.sh 2>&1 | tee deploy.log`
2. Скрипт автоматически откатится к предыдущей версии
3. Проверьте доступ к серверу и права на файлы

### Проблема: Nginx не применяет новую конфигурацию

**Решение:**
```bash
# Проверить синтаксис
sshpass -p '$SERVER_PASSWORD' ssh root@95.163.227.26 "nginx -t"

# Перезагрузить
sshpass -p '$SERVER_PASSWORD' ssh root@95.163.227.26 "systemctl reload nginx"
```

## 📞 Support

При возникновении проблем:
1. Проверьте этот гайд
2. Проверьте логи сервера
3. Проверьте бэкапы и при необходимости откатитесь
