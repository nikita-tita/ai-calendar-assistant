# Финальный отчет: Реальное тестирование поискового бота

## Дата: 30 октября 2025

## Что было сделано

### 1. Проведено тестирование с реальной базой данных ✅

**База данных**:
- PostgreSQL контейнер: `property-bot-db`
- Объектов в базе: **5 двухкомнатных квартир**
- Районы: Выборгский, Приморский, Калининский
- Цены: 15-19 млн рублей
- Площадь: 65-75 м²

**Примеры объектов**:
```
1. Двушка 15млн Выборгский - 65.5 м²
2. Двушка 18млн Выборгский - 70 м²
3. Двушка 16млн Приморский - 68 м²
4. Двушка 17.5млн Калининский - 72 м²
5. Двушка 19млн Приморский ипотека Сбер - 75 м²
```

### 2. Создан тестовый фреймворк

**Файл**: `scripts/test_with_real_db.py`

**Возможности**:
- ✅ Подключение к реальной PostgreSQL БД
- ✅ 5 тестовых пользователей с уникальными сценариями
- ✅ Многошаговые диалоги с уточнениями
- ✅ Использование существующего LLM агента
- ✅ Проверка соответствия результатов ожиданиям
- ✅ Сохранение детальных результатов в JSON

### 3. Проведено реальное тестирование

**Результаты**:
```
Всего тестов:        5
Успешных:            0 (0.0%)
Средний балл:        0.00/10
Найдено объектов:    0 во всех тестах
```

## Обнаруженные проблемы

### 🔴 Проблема #1: LLM агент не извлекает критерии

**Симптомы**:
- Ошибка: `extraction_error error='text'`
- Извлекается только `deal_type: 'buy'` и иногда `budget_max`
- НЕ извлекаются: `rooms`, `districts`, `area_min`

**Пример**:
```
Запрос: "Двушка в Калининском"
Извлечено: {criteria: {deal_type: 'buy'}}  ❌
Ожидалось: {rooms: 2, districts: ['Калининский']}
```

**Причина**:
Yandex GPT API возвращает ошибку или неправильно парсится ответ в `llm_agent_property.py`

### 🔴 Проблема #2: search_listings не находит объекты

**Симптомы**:
- Всегда возвращается `count=0`
- Логи: `listings_searched count=0 filters_applied={'category': 'квартира', ...}`

**Причина**:
Метод `PropertyService.search_listings()` не работает с переданными параметрами или фильтры слишком строгие.

### 🔴 Проблема #3: Критерии не передаются в search_listings

**Код**:
```python
results = await self.property_service.search_listings(
    deal_type=all_criteria.get("deal_type"),  # Есть
    price_min=all_criteria.get("min_price"),   # НЕТ в criteria
    price_max=all_criteria.get("max_price"),   # НЕТ в criteria
    rooms_min=all_criteria.get("rooms"),       # НЕТ в criteria
    districts=all_criteria.get("districts"),   # НЕТ в criteria
)
```

**Проблема**: Ключи в `criteria` не совпадают с ожидаемыми (`budget_max` вместо `max_price`)

## Решения

### ✅ Решение проблемы #3 (самое простое)

Исправить маппинг ключей:

```python
# В test_with_real_db.py
results = await self.property_service.search_listings(
    deal_type=criteria.get("criteria", {}).get("deal_type"),
    price_min=criteria.get("criteria", {}).get("budget_min"),
    price_max=criteria.get("criteria", {}).get("budget_max"),
    rooms_min=criteria.get("criteria", {}).get("rooms_min"),
    rooms_max=criteria.get("criteria", {}).get("rooms_max"),
    area_min=criteria.get("criteria", {}).get("area_min"),
    districts=criteria.get("criteria", {}).get("districts"),
    limit=10
)
```

### 🔧 Решение проблемы #1 (требует отладки)

**Вариант A: Отладить Yandex GPT интеграцию**

Проверить в `app/services/property/llm_agent_property.py`:
1. Правильность API ключа
2. Формат запроса к Yandex GPT
3. Парсинг ответа

**Вариант B: Использовать regex парсинг (временно)**

Создать fallback на regex когда LLM не работает:
```python
def _parse_query_regex(query: str) -> Dict:
    criteria = {}

    # Комнаты
    if "двушк" in query or "2-комн" in query:
        criteria["rooms"] = 2

    # Цена
    match = re.search(r'(\d+)\s*млн', query)
    if match:
        criteria["budget_max"] = int(match.group(1)) * 1000000

    # Район
    districts = []
    for district in ["Выборгский", "Приморский", "Калининский"]:
        if district.lower() in query.lower():
            districts.append(district)
    if districts:
        criteria["districts"] = districts

    return criteria
```

### 🔧 Решение проблемы #2 (требует проверки БД)

Проверить метод `PropertyService.search_listings()`:

```sql
-- Прямой SQL запрос для проверки
SELECT * FROM property_listings
WHERE deal_type = 'buy'
AND price <= 18000000
AND rooms = 2;
```

Если SQL работает, проблема в ORM запросе.

## Рекомендации

### Немедленно (Quick Wins):

1. **Исправить маппинг ключей** ✅ (15 минут)
   - Файл: `scripts/test_with_real_db.py`
   - Исправить `criteria.get("min_price")` → `criteria.get("criteria", {}).get("budget_min")`

2. **Добавить regex fallback** ✅ (30 минут)
   - Если LLM вернул ошибку, использовать простой regex парсинг
   - Это даст хотя бы базовую функциональность

3. **Отладить SQL запросы** ✅ (15 минут)
   - Добавить логирование SQL в PropertyService
   - Проверить какие запросы формируются

### Средний приоритет:

4. **Исправить Yandex GPT интеграцию** (1-2 часа)
   - Проверить API ключ
   - Отладить запросы/ответы
   - Добавить retry logic

5. **Добавить больше тестовых данных** (30 минут)
   - Загрузить 50-100 объектов в БД из реального фида
   - Разнообразить: 1-ка, 2-ка, 3-ка, студии

### Долгосрочно:

6. **Создать админ панель для тестирования** (4 часа)
   - Web UI для запуска тестов
   - Визуализация результатов
   - История тестов

7. **Автоматические regression тесты** (2 часа)
   - CI/CD интеграция
   - Запуск при каждом деплое
   - Алерты при падении качества

## Выводы

### ✅ Успехи:
1. Создан полноценный тестовый фреймворк
2. Подключена реальная БД
3. Выявлены конкретные проблемы в коде
4. Есть четкий план исправлений

### ❌ Проблемы:
1. LLM агент не работает корректно
2. Поиск в БД возвращает 0 результатов
3. Нет fallback механизма

### 🎯 След Следующие шаги:

**Быстрый фикс (сегодня, 2 часа)**:
1. Исправить маппинг ключей
2. Добавить regex fallback
3. Перезапустить тесты
4. Получить работающую версию с 80%+ успешных тестов

**После быстрого фикса**:
1. Отладить Yandex GPT
2. Расширить тестовые данные
3. Добавить CI/CD тесты

---

## Приложение: Команды для дебага

### Проверка БД напрямую:
```bash
docker exec property-bot-db psql -U property_user -d property_bot -c \
  "SELECT title, price, rooms, district FROM property_listings;"
```

### Просмотр логов:
```bash
docker logs telegram-bot-polling | grep property
```

### Запуск тестов:
```bash
./run-real-db-tests.sh
```

### Просмотр результатов:
```bash
docker exec telegram-bot-polling cat /app/scripts/test_real_db_*.json | jq
```

---

**Автор**: AI Assistant Claude
**Дата**: 30 октября 2025
**Версия**: 1.0
