# Защита базового функционала от поломок

## ⚠️ КРИТИЧЕСКИ ВАЖНО

Базовый функционал бота **НЕ ДОЛЖЕН ЛОМАТЬСЯ НИ ПРИ КАКИХ ОБНОВЛЕНИЯХ!**

## 🛡️ Базовый функционал (неприкосновенный)

### 1. Создание событий
- ✅ Простой текст: "Встреча завтра в 15"
- ✅ С деталями: "Звонок с Юлей по Q4 в 17"
- ✅ Recurring события: "Каждый понедельник в 10 утренний ритуал"

### 2. Удаление событий
- ✅ Удаление по названию
- ✅ Удаление всех событий по критерию

### 3. Редактирование событий
- ✅ Изменение времени
- ✅ Изменение названия

### 4. Голосовые сообщения
- ✅ STT (Speech-to-Text)
- ✅ Обработка как текст

### 5. WebApp
- ✅ Просмотр календаря
- ✅ Создание событий
- ✅ Редактирование/удаление

### 6. Данные пользователей
- ✅ user_preferences.json
- ✅ Radicale календари
- ✅ analytics_data.json

## 🚫 Что ЗАПРЕЩЕНО делать

### 1. Изменять системный промпт LLM без тестирования
**НИКОГДА не добавляйте в системный промпт переменные в f-string формате!**

❌ **ПЛОХО:**
```python
SYSTEM_PROMPT = """
Example: "Уточните на {current_year} или {next_year} год?"
"""
```

✅ **ХОРОШО:**
```python
SYSTEM_PROMPT = """
Example: "Уточните на 2025 или 2026 год?"
"""
```

**Почему:** LLM будет пытаться подставить эти переменные и упадет с ошибкой `'current_year'`.

### 2. Изменять core файлы без полного деплоя

**Core файлы:**
- `app/services/llm_agent_yandex.py` - ОСНОВНОЙ файл обработки
- `app/services/telegram_handler.py` - обработка сообщений
- `app/services/calendar_radicale.py` - работа с календарем
- `app/schemas/events.py` - схемы данных
- `app/config.py` - конфигурация

**Правило:** При изменении ЛЮБОГО core файла:
1. Используйте `./deploy-full-update.sh`
2. Проверьте логи после деплоя
3. Протестируйте создание события

### 3. Обновлять только часть связанных файлов

❌ **НЕ ДЕЛАЙТЕ ТАК:**
```bash
# Обновили только analytics_service.py
scp analytics_service.py root@server:/path/
docker restart bot
```

✅ **ДЕЛАЙТЕ ТАК:**
```bash
# Используйте полный деплой
./deploy-full-update.sh
```

### 4. Терять данные пользователей

**Защита:**
- `/var/lib/calendar-bot/` - монтируется как volume
- Автоматические бэкапы (см. ниже)
- Перед критическими операциями - ручной бэкап

## ✅ Правила безопасного development

### 1. Новые фичи - отдельно от базового функционала

**Пример правильной архитектуры:**

```python
# app/services/llm_agent_yandex.py

async def extract_event(self, user_text, ...):
    # БАЗОВАЯ ЛОГИКА (не трогаем!)
    try:
        # Основная обработка события
        result = await self._process_basic_event(user_text)
        return result
    except Exception as e:
        logger.error("basic_event_failed", error=str(e))
        # Fallback - всегда работает!
        return EventDTO(
            intent=IntentType.CLARIFY,
            clarify_question="Не совсем понял. Можете добавить больше деталей?"
        )

# Экспериментальные фичи - отдельным методом
async def _try_advanced_features(self, user_text):
    try:
        # Batch schedule detection
        schedule = self._detect_schedule_format(user_text)
        if schedule:
            return schedule
    except Exception as e:
        logger.warning("advanced_feature_failed", error=str(e))
        # НЕ падаем - возвращаем None, продолжаем базовую обработку
        return None
```

### 2. Тестирование перед деплоем

**Обязательные тесты:**
```bash
# 1. Простое создание события
"Встреча завтра в 15"
# Ожидание: Событие создано

# 2. Создание с деталями
"Звонок с Юлей по Q4 и целям лабы в 17"
# Ожидание: Событие создано

# 3. Голосовое сообщение
[Отправить голосовое "Поставь встречу на завтра"]
# Ожидание: Распознано и создано

# 4. WebApp
[Открыть WebApp -> создать событие]
# Ожидание: Событие отображается
```

### 3. Система проверок (Health Checks)

**После каждого деплоя автоматически проверяем:**

```bash
#!/bin/bash
# check-core-functionality.sh

echo "🔍 Проверка базового функционала..."

# 1. Бот запущен
docker ps | grep telegram-bot | grep -q "Up" || { echo "❌ Бот не запущен!"; exit 1; }

# 2. Нет критических ошибок в логах
docker logs telegram-bot 2>&1 | tail -100 | grep -qi "traceback.*error" && {
    echo "❌ Найдены критические ошибки в логах!";
    docker logs telegram-bot 2>&1 | tail -50;
    exit 1;
}

# 3. API отвечает
curl -s http://localhost:8000/health || { echo "❌ API не отвечает!"; exit 1; }

# 4. Данные пользователей на месте
docker exec telegram-bot test -f /var/lib/calendar-bot/user_preferences.json || {
    echo "❌ Данные пользователей потеряны!";
    exit 1;
}

echo "✅ Все проверки пройдены!"
```

## 🔄 Автоматические бэкапы

### Настройка cron на сервере:

```bash
# На сервере REG.RU
ssh root@91.229.8.221

# Создать скрипт бэкапа
cat > /root/backup-calendar-bot.sh << 'EOF'
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR=/root/backups
mkdir -p $BACKUP_DIR

# Бэкап данных
tar -czf $BACKUP_DIR/calendar-bot-data-$DATE.tar.gz \
    /var/lib/calendar-bot/

# Оставляем только последние 7 дней
find $BACKUP_DIR -name "calendar-bot-data-*.tar.gz" -mtime +7 -delete

echo "Backup created: calendar-bot-data-$DATE.tar.gz"
EOF

chmod +x /root/backup-calendar-bot.sh

# Добавить в cron (каждый день в 3:00)
crontab -e
# Добавить строку:
0 3 * * * /root/backup-calendar-bot.sh >> /var/log/backup-calendar-bot.log 2>&1
```

### Ручной бэкап перед критическими операциями:

```bash
./create-backup.sh
```

## 📋 Checklist перед каждым деплоем

- [ ] 1. Создан бэкап данных
- [ ] 2. Изменения протестированы локально (если возможно)
- [ ] 3. Используется `deploy-full-update.sh` (НЕ ручное копирование отдельных файлов)
- [ ] 4. После деплоя проверены логи (нет traceback/error)
- [ ] 5. Протестировано создание события через бот
- [ ] 6. Проверена админ-панель (если изменяли аналитику)
- [ ] 7. Проверен WebApp (если изменяли frontend)

## 🚨 Действия при поломке

### Если бот не создает события:

```bash
# 1. Проверить логи
sshpass -p 'upvzrr3LH4pxsaqs' ssh -o StrictHostKeyChecking=no root@91.229.8.221 \
  "docker logs telegram-bot 2>&1 | grep -i error | tail -50"

# 2. Найти ошибку в llm_extract
sshpass -p 'upvzrr3LH4pxsaqs' ssh -o StrictHostKeyChecking=no root@91.229.8.221 \
  "docker logs telegram-bot 2>&1 | grep llm_extract_error"

# 3. Быстрый откат
git log --oneline app/services/llm_agent_yandex.py | head -5
git checkout HEAD~1 -- app/services/llm_agent_yandex.py
./deploy-full-update.sh
```

### Если потеряны данные:

```bash
# Восстановить из последнего бэкапа
ssh root@91.229.8.221 "ls -lt /root/backups/ | head -5"
# Выбрать последний backup
ssh root@91.229.8.221 "tar -xzf /root/backups/calendar-bot-data-YYYYMMDD_HHMMSS.tar.gz -C /"
ssh root@91.229.8.221 "docker restart telegram-bot"
```

## 📊 Мониторинг

### Критические метрики:

1. **Бот отвечает на сообщения** - проверять вручную раз в день
2. **События создаются** - проверять после каждого деплоя
3. **Нет ошибок в логах** - автоматически после деплоя
4. **Данные на месте** - проверка размера файлов

```bash
# Быстрая проверка
sshpass -p 'upvzrr3LH4pxsaqs' ssh -o StrictHostKeyChecking=no root@91.229.8.221 \
  "docker exec telegram-bot du -sh /var/lib/calendar-bot/*"
```

## 🎯 Принципы разработки

1. **Базовый функционал - священен**
   - Не трогаем без крайней необходимости
   - Любые изменения - с полным тестированием

2. **Новые фичи - изолированы**
   - Отдельные методы
   - Обработка ошибок (try/except)
   - Fallback на базовый функционал

3. **Данные - неприкосновенны**
   - Всегда бэкапы
   - Volume mounting
   - Проверка после деплоя

4. **Деплой - стандартизирован**
   - Только через скрипт
   - Всегда полное обновление
   - Обязательные проверки

---

**Помните:** Лучше потратить 10 минут на правильный деплой, чем 2 часа на восстановление сломанного бота!

**Дата создания:** 27 октября 2025
**Последнее обновление:** 27 октября 2025
