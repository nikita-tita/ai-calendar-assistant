# Руководство по Smoke-тестированию

**Последнее обновление:** 2025-12-04

---

## Обзор

Этот документ описывает процедуру smoke-тестирования AI Calendar Assistant для агентов по недвижимости. Цель — убедиться, что все основные сценарии работают корректно.

---

## Целевая аудитория

**Агенты по недвижимости** — риелторы, которые:
- Проводят показы квартир
- Встречаются с собственниками и покупателями
- Ведут много параллельных сделок
- Часто за рулём и используют голосовой ввод
- Нуждаются в быстром планировании без сложных интерфейсов

---

## Подготовка к тестированию

### 1. Проверка доступности системы

```bash
# Health check
curl https://calendar.housler.ru/health
# Ожидаемый ответ: {"status":"ok","version":"0.1.0"}

# Версия WebApp
curl -s https://calendar.housler.ru/static/index.html | grep "APP_VERSION"
```

### 2. Проверка логов на ошибки

```bash
ssh -i ~/.ssh/id_housler root@95.163.227.26 'docker logs telegram-bot --tail 100 2>&1 | grep -iE "error|exception"'
# Ожидание: пустой вывод (ошибок нет)
```

### 3. Проверка контейнеров

```bash
ssh -i ~/.ssh/id_housler root@95.163.227.26 'docker ps | grep -E "(telegram-bot|redis|radicale)"'
# Все 3 контейнера должны быть в статусе "healthy"
```

---

## Сценарии тестирования

## Сценарий 1: Создание события с конкретным временем

### Описание
Агент планирует показ квартиры с указанием точного времени.

### Тестовые фразы

| Фраза пользователя | Ожидаемый intent | Ожидаемый ответ |
|-------------------|------------------|-----------------|
| "Показ квартиры завтра в 14:00" | `create` | ✅ Записал<br>05.12.2025 14:00 • Показ квартиры |
| "Встреча с клиентом в пятницу в 10 утра" | `create` | ✅ Записал<br>06.12.2025 10:00 • Встреча с клиентом |
| "Подписание договора 10 декабря в 16:00 в офисе" | `create` | ✅ Записал<br>10.12.2025 16:00 • Подписание договора (офис) |
| "Завтра в 17 встреча с Ивановым по квартире на Ленина" | `create` | ✅ Записал<br>05.12.2025 17:00 • Встреча с Ивановым по квартире на Ленина |

### Проверка в логах

```bash
ssh -i ~/.ssh/id_housler root@95.163.227.26 'docker logs telegram-bot 2>&1 | grep "llm_extract_success" | tail -5'
# Ожидание: intent=create, confidence > 0.7
```

---

## Сценарий 2: Создание задачи (TODO) без времени

### Описание
Агент добавляет задачу, которую нужно сделать, но без привязки к конкретному времени.

### Тестовые фразы

| Фраза пользователя | Ожидаемый intent | Ожидаемый ответ |
|-------------------|------------------|-----------------|
| "Позвонить собственнику по Пушкина 15" | `todo` | ✅ Добавил в задачи: Позвонить собственнику по Пушкина 15 |
| "Подготовить документы для сделки" | `todo` | ✅ Добавил в задачи: Подготовить документы для сделки |
| "Сфотографировать квартиру на Мира" | `todo` | ✅ Добавил в задачи: Сфотографировать квартиру на Мира |
| "Обновить объявление на Циан" | `todo` | ✅ Добавил в задачи: Обновить объявление на Циан |
| "Договориться о встрече с оценщиком" | `todo` | ✅ Добавил в задачи: Договориться о встрече с оценщиком |
| "Проверить историю квартиры в Росреестре" | `todo` | ✅ Добавил в задачи: Проверить историю квартиры в Росреестре |

### Ключевое отличие от событий
- **Событие**: есть конкретное время ("в 14:00", "завтра в 10")
- **Задача**: нет времени, только действие ("позвонить", "подготовить", "проверить")

---

## Сценарий 3: Просмотр расписания

### Описание
Агент хочет узнать свои планы на определённый период.

### Тестовые фразы

| Фраза пользователя | Ожидаемый intent | Ожидаемый ответ |
|-------------------|------------------|-----------------|
| "Что у меня сегодня?" | `query` | 📅 Ваши события:<br>• 10:00 - Показ на Ленина<br>• 14:00 - Встреча с покупателем |
| "Какие планы на завтра?" | `query` | 📅 Ваши события:<br>• 11:00 - Подписание договора<br>... |
| "Покажи расписание на неделю" | `query` | 📅 Ваши события:<br>(список на 7 дней) |
| "Что запланировано на пятницу?" | `query` | 📅 Ваши события на пятницу:<br>... |

### Кнопки быстрого доступа
В интерфейсе есть кнопки:
- 📋 Дела на сегодня
- 📅 Дела на завтра
- ✅ Задачи

---

## Сценарий 4: Поиск свободного времени

### Описание
Агент ищет окно для нового показа.

### Тестовые фразы

| Фраза пользователя | Ожидаемый intent | Ожидаемый ответ |
|-------------------|------------------|-----------------|
| "Когда я свободен завтра?" | `find_free_slots` | Свободно 5 декабря:<br>• 09:00–10:00 (1ч)<br>• 12:00–14:00 (2ч)<br>• 16:00–18:00 (2ч) |
| "Есть свободные слоты в пятницу после обеда?" | `find_free_slots` | Свободно 6 декабря:<br>• 14:00–15:30 (1ч 30м)<br>• 17:00–19:00 (2ч) |
| "Когда могу вставить показ на 1.5 часа?" | `find_free_slots` | (слоты >= 90 минут) |

---

## Сценарий 5: Изменение события

### Описание
Агент переносит или меняет детали встречи.

### Тестовые фразы

| Фраза пользователя | Ожидаемый intent | Ожидаемый ответ |
|-------------------|------------------|-----------------|
| "Перенеси показ на Ленина на 16:00" | `update` | ✅ Изменил<br>📅 Показ на Ленина<br>🕐 05.12.2025 16:00 |
| "Сдвинь встречу с Петровым на час позже" | `update` | ✅ Изменил<br>📅 Встреча с Петровым<br>🕐 (новое время) |
| "Измени место встречи на кафе Пушкин" | `update` | ✅ Изменил<br>📅 ...<br>📍 кафе Пушкин |

### Если событие не найдено

| Фраза | Ответ |
|-------|-------|
| "Перенеси встречу" | Не понял, какое событие менять. Уточните. |

---

## Сценарий 6: Удаление события

### Описание
Агент отменяет запланированную встречу.

### Тестовые фразы

| Фраза пользователя | Ожидаемый intent | Ожидаемый ответ |
|-------------------|------------------|-----------------|
| "Отмени показ на Ленина" | `delete` | ✅ Событие удалено!<br>📅 Показ на Ленина<br>🕐 05.12.2025 14:00 |
| "Удали встречу с Ивановым завтра" | `delete` | ✅ Событие удалено!<br>... |

---

## Сценарий 7: Повторяющиеся события

### Описание
Агент создаёт регулярные встречи или звонки.

### Тестовые фразы

| Фраза пользователя | Ожидаемый intent | Ожидаемый ответ |
|-------------------|------------------|-----------------|
| "Каждый понедельник в 10 утра планёрка" | `create_recurring` | ✅ Создано еженедельное событие<br>Понедельник 10:00 • Планёрка<br>Создано повторений: 4 |
| "Ежедневно в 9 утра обзвон базы" | `create_recurring` | ✅ Создано ежедневное событие<br>09:00 • Обзвон базы<br>Создано повторений: 30 |
| "Каждую пятницу в 18:00 отчёт руководителю" | `create_recurring` | ✅ Создано еженедельное событие<br>... |

---

## Сценарий 8: Пакетное создание (расписание)

### Описание
Агент вставляет несколько событий сразу из расписания.

### Тестовая фраза

```
Тайминг на завтра:
10:00-11:00 Показ на Ленина 15
11:30-12:30 Показ на Мира 8
14:00-15:00 Встреча с собственником
16:00-17:00 Подписание в МФЦ
```

### Ожидаемый intent
`batch_confirm`

### Ожидаемый ответ

```
📅 Создать 4 события:

• Показ на Ленина 15
  📅 05.12 | 🕐 10:00-11:00
• Показ на Мира 8
  📅 05.12 | 🕐 11:30-12:30
• Встреча с собственником
  📅 05.12 | 🕐 14:00-15:00
• Подписание в МФЦ
  📅 05.12 | 🕐 16:00-17:00
```

После подтверждения:
```
✅ Записал:

• 05.12.2025 10:00 — Показ на Ленина 15
• 05.12.2025 11:30 — Показ на Мира 8
• 05.12.2025 14:00 — Встреча с собственником
• 05.12.2025 16:00 — Подписание в МФЦ
```

---

## Сценарий 9: Несколько действий в одном сообщении

### Описание
Агент говорит несколько дел одной фразой.

### Тестовые фразы

| Фраза | Ожидаемый intent | Результат |
|-------|------------------|-----------|
| "В 17 встреча с клиентом, в 19 ужин с женой" | `batch_confirm` | 2 события создано |
| "Завтра в 10 показ, потом в 12 подписание, и ещё позвонить юристу" | `batch_confirm` | 2 события + 1 задача |
| "В 14 показ на Мира, потом нужно отправить договор" | `batch_confirm` | 1 событие + 1 задача |

---

## Сценарий 10: Массовое удаление

### Описание
Агент хочет удалить несколько событий по критерию.

### Тестовые фразы

| Фраза | Ожидаемый intent | Ожидаемый ответ |
|-------|------------------|-----------------|
| "Удали все показы на этой неделе" | `delete_by_criteria` | Найдено: 5<br>Удалить:<br>• Показ на Ленина (05.12)<br>• Показ на Мира (06.12)<br>...<br>[✅ Удалить] [❌ Отмена] |
| "Удали дубликаты" | `delete_duplicates` | Найдено дубликатов: 3<br>[✅ Удалить] [❌ Отмена] |

---

## Сценарий 11: Уточняющие вопросы

### Описание
Система просит уточнить, если информации недостаточно.

### Тестовые фразы

| Фраза | Ожидаемый intent | Ожидаемый ответ |
|-------|------------------|-----------------|
| "Запиши встречу" | `clarify` | Уточните, пожалуйста, дату и время встречи. |
| "Показ квартиры" (без времени) | `todo` или `clarify` | Добавил в задачи: Показ квартиры<br>или<br>Уточните время показа |
| "Перенеси" | `clarify` | Какое событие перенести и на какое время? |

---

## Сценарий 12: Голосовой ввод

### Описание
Агент за рулём диктует голосом.

### Процесс
1. Пользователь отправляет голосовое сообщение
2. Система отвечает: "🎤 Слушаю..."
3. Распознаёт речь через Yandex SpeechKit
4. Показывает: `Вы: "распознанный текст"`
5. Обрабатывает как текстовое сообщение

### Типичные голосовые команды агентов

- "Запиши показ на завтра в три часа дня"
- "Что у меня сегодня"
- "Позвонить Петрову по квартире"
- "Перенеси встречу на пятницу"

---

## Проверка через логи

### Успешная обработка запроса

```bash
ssh -i ~/.ssh/id_housler root@95.163.227.26 'docker logs telegram-bot 2>&1 | grep -E "(text_message|llm_extract)" | tail -10'
```

Ожидаемый вывод:
```
[info] text_message_received    text=Показ завтра в 14:00 user_id=123456
[info] llm_extract_start_yandex user_text=Показ завтра в 14:00
[info] llm_extract_success_yandex intent=create confidence=0.9
```

### Проверка ошибок

```bash
ssh -i ~/.ssh/id_housler root@95.163.227.26 'docker logs telegram-bot 2>&1 | grep -iE "error|exception|failed" | tail -10'
```

Ожидание: пустой вывод (ошибок нет)

---

## Чек-лист для ручного тестирования

### Базовые операции

- [ ] Создание события с временем
- [ ] Создание задачи без времени
- [ ] Просмотр событий на сегодня (кнопка)
- [ ] Просмотр событий на завтра (кнопка)
- [ ] Просмотр задач (кнопка)
- [ ] Поиск свободных слотов
- [ ] Изменение события
- [ ] Удаление события

### Продвинутые операции

- [ ] Повторяющиеся события
- [ ] Пакетное создание из расписания
- [ ] Несколько действий в одном сообщении
- [ ] Массовое удаление по критерию
- [ ] Удаление дубликатов

### Интерфейс

- [ ] WebApp открывается (кнопка "🗓 Кабинет")
- [ ] Скролл к сегодняшней дате
- [ ] Создание события через WebApp
- [ ] Создание задачи через WebApp
- [ ] Настройки часового пояса

### Голосовой ввод

- [ ] Распознавание голоса
- [ ] Показ распознанного текста
- [ ] Корректная обработка команды

---

## Критерии успешности

### Smoke-тест пройден, если:

1. **Health check** возвращает `{"status":"ok"}`
2. **Все 3 контейнера** в статусе healthy
3. **Нет ошибок** в логах за последний час
4. **Базовые сценарии** (1-6) работают корректно
5. **Голосовой ввод** распознаёт речь
6. **WebApp** открывается и отображает события

### Smoke-тест провален, если:

1. Health check не отвечает или возвращает ошибку
2. Любой контейнер не healthy
3. Есть критические ошибки в логах
4. Создание события не работает
5. Просмотр расписания не работает
6. WebApp не открывается

---

## Автоматизированная проверка

```bash
#!/bin/bash
# smoke_test.sh

echo "=== AI Calendar Assistant Smoke Test ==="

# 1. Health check
echo -n "1. Health check: "
HEALTH=$(curl -s https://calendar.housler.ru/health)
if [[ "$HEALTH" == *"ok"* ]]; then
    echo "✅ PASSED"
else
    echo "❌ FAILED: $HEALTH"
    exit 1
fi

# 2. WebApp
echo -n "2. WebApp available: "
CODE=$(curl -s -o /dev/null -w "%{http_code}" https://calendar.housler.ru/app)
if [[ "$CODE" == "200" ]]; then
    echo "✅ PASSED"
else
    echo "❌ FAILED: HTTP $CODE"
fi

# 3. Containers (requires SSH)
echo -n "3. Containers healthy: "
CONTAINERS=$(ssh -i ~/.ssh/id_housler root@95.163.227.26 'docker ps --format "{{.Names}}: {{.Status}}" | grep -E "(telegram-bot|redis|radicale)"' 2>/dev/null)
if [[ "$CONTAINERS" == *"healthy"*"healthy"*"healthy"* ]]; then
    echo "✅ PASSED"
else
    echo "⚠️  Check manually"
fi

# 4. No errors in logs
echo -n "4. No errors in logs: "
ERRORS=$(ssh -i ~/.ssh/id_housler root@95.163.227.26 'docker logs telegram-bot --since 1h 2>&1 | grep -ciE "error|exception"' 2>/dev/null)
if [[ "$ERRORS" == "0" ]]; then
    echo "✅ PASSED"
else
    echo "⚠️  $ERRORS errors found"
fi

echo "=== Smoke Test Complete ==="
```

---

## Частые проблемы и решения

### Проблема: "Не разобрал" на голосовое

**Причина:** Плохое качество аудио или тихая речь
**Решение:** Говорить громче и чётче, избегать фонового шума

### Проблема: Неправильная дата

**Причина:** Неверный часовой пояс
**Решение:** Настроить через /timezone или ⚙️ Настройки

### Проблема: "Уточните время" на задачу

**Причина:** GPT не распознал как задачу
**Решение:** Добавить слово "задача" или "надо": "Задача: позвонить клиенту"

### Проблема: WebApp не обновляется

**Причина:** Кэш Telegram
**Решение:** Закрыть и открыть WebApp заново, или очистить данные бота

---

## См. также

- [ARCHITECTURE.md](../01-core/ARCHITECTURE.md) — архитектура системы
- [CHANGELOG.md](../../CHANGELOG.md) — история изменений
- [CLAUDE.md](../../CLAUDE.md) — инструкции для разработки
