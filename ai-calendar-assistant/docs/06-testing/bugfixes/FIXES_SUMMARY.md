# 🔧 Исправления и Обновления

## Дата: 2025-10-28

---

## ✅ Проблема 1: Отвалился календарный бот

### Симптомы
```
User: "📋 Дела на сегодня"
Bot: "⚠️ Календарный сервис временно недоступен. Попробуйте позже."
```

Веб-приложение не показывало события.

### Диагностика

Проверка логов показала:
```
calendar_error: Failed to resolve 'radicale'
[Errno -3] Temporary failure in name resolution
```

Контейнер Radicale был остановлен.

### Решение

1. Запущен контейнер Radicale:
```bash
docker-compose up -d radicale
```

2. Перезапущен telegram-bot для переподключения:
```bash
docker restart telegram-bot
```

3. Проверен статус:
```bash
docker ps | grep -E "telegram-bot|radicale"
# ✅ Оба контейнера работают

docker logs telegram-bot | grep calendar_found
# ✅ Календари найдены для всех пользователей
```

### Результат
✅ Календарный бот восстановлен
✅ События отображаются в веб-приложении
✅ Все CRUD операции работают

---

## ✅ Проблема 2: Разделение AI агентов

### Требование

> "Оба бота должны работать на отдельных AI агентах, т.е. никакого пересечения в промптах"

### Что было

Один агент `llm_agent_yandex` обрабатывал все запросы. Потенциальный конфликт промптов между календарём и недвижимостью.

### Что сделано

#### 1. Создан отдельный Property Agent

**Файл:** [app/services/llm_agent_property.py](app/services/llm_agent_property.py:1)

**Промпт:**
```python
"""
Ты - AI-агент для поиска недвижимости (новостроек).

ВАЖНО: Ты работаешь ТОЛЬКО с поиском недвижимости.
НЕ обрабатывай запросы о календаре, событиях, встречах и т.п.

Извлекай параметры:
- Бюджет (budget_min, budget_max)
- Комнаты (rooms_min, rooms_max)
- Локация (districts, metro_stations)
- Этаж, лифт, животные и т.д.

Если запрос НЕ о недвижимости:
{
  "intent": "out_of_scope",
  "message": "Я помогаю только с поиском недвижимости"
}
"""
```

#### 2. Calendar Agent остался отдельным

**Файл:** [app/services/llm_agent_yandex.py](app/services/llm_agent_yandex.py:1)

**Промпт:**
```python
"""
Ты - AI-ассистент календаря.

Ты помогаешь с:
- Создание/изменение/удаление событий
- Поиск свободного времени
- Управление расписанием

Ты НЕ работаешь с поиском недвижимости.
"""
```

#### 3. Логика переключения

**Файл:** [app/services/telegram_handler.py](app/services/telegram_handler.py:179-194)

```python
# Check if user wants to switch to property mode
if message.text in ["🏠 Поиск новостройки", "Поиск новостройки"]:
    await property_handler.handle_mode_switch(update, user_id, BotMode.PROPERTY)
    return

# Check current bot mode
current_mode = await property_service.get_user_mode(user_id)

if current_mode == BotMode.PROPERTY:
    # Use Property Agent (llm_agent_property)
    await property_handler.handle_property_message(update, user_id, message.text)
else:
    # Use Calendar Agent (llm_agent_yandex)
    await self._handle_text(update, user_id, message.text)
```

### Архитектура

```
User Message
    ↓
Telegram Handler
    ↓
Check Bot Mode (from DB)
    ↓           ↓
CALENDAR    PROPERTY
    ↓           ↓
Calendar    Property
Handler     Handler
    ↓           ↓
llm_agent   llm_agent
_yandex     _property
(Календарь) (Недвижимость)
```

### Хранение режима

```sql
CREATE TABLE user_bot_modes (
    telegram_user_id TEXT PRIMARY KEY,
    current_mode TEXT,  -- 'calendar' or 'property'
    active_client_id TEXT,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

### Примеры работы

#### Пример 1: Календарь

```
User: "Создай встречу завтра в 15:00"
Mode: CALENDAR
Agent: llm_agent_yandex
↓
Result: IntentType.CREATE, start_time="2025-10-29 15:00"
↓
✅ Событие создано
```

#### Пример 2: Недвижимость

```
User: "Ищу 2-комнатную в Бутово до 10 млн"
Mode: PROPERTY
Agent: llm_agent_property
↓
Result: intent="search", criteria={rooms: 2, budget: 10M}
↓
✅ Подборка квартир
```

#### Пример 3: Неверный контекст

```
User в режиме PROPERTY: "Создай встречу"
Agent: llm_agent_property
↓
Result: intent="out_of_scope"
↓
Response: "Я помогаю только с поиском недвижимости.
Для работы с календарём нажмите '📅 Календарь'."
```

### Результат

✅ Полное разделение агентов
✅ Нет конфликтов в промптах
✅ Каждый агент специализируется на своей задаче
✅ Независимая оптимизация каждого агента
✅ Автоматическое определение out-of-scope запросов

---

## 📦 Созданные файлы

### Новые файлы

1. **app/models/property.py** (217 строк)
   - SQLAlchemy модели для недвижимости

2. **app/schemas/property.py** (267 строк)
   - Pydantic схемы валидации

3. **app/services/property_service.py** (460 строк)
   - CRUD операции с БД

4. **app/services/property_scoring.py** (447 строк)
   - Система скоринга Dream Score

5. **app/services/property_handler.py** (604 строки)
   - Telegram обработчик для недвижимости

6. **app/services/llm_agent_property.py** (365 строк)
   - **Отдельный AI агент для недвижимости**

7. **app/routers/property.py** (267 строк)
   - REST API endpoints

8. **AI_AGENTS_SEPARATION.md**
   - Документация по разделению агентов

9. **deploy-property-bot.sh**
   - Скрипт развертывания

### Изменённые файлы

1. **app/services/telegram_handler.py**
   - Добавлена логика переключения режимов
   - Интеграция с property_handler

2. **app/main.py**
   - Подключен property router

---

## 🚀 Развертывание

### Локально

```bash
# Установить зависимости (если нужно)
pip install -r requirements.txt

# Запустить
python -m app.main
```

### На сервер

```bash
# Автоматическое развертывание
./deploy-property-bot.sh

# Или вручную
scp app/services/llm_agent_property.py root@91.229.8.221:/root/ai-calendar-assistant/app/services/
scp app/services/property_handler.py root@91.229.8.221:/root/ai-calendar-assistant/app/services/
# ... остальные файлы

ssh root@91.229.8.221
cd /root/ai-calendar-assistant
docker restart telegram-bot
```

---

## 🧪 Тестирование

### 1. Проверить календарь

```
Telegram:
1. Откройте бота
2. Нажмите "📋 Дела на сегодня"
✅ Должны отобразиться события

Веб:
1. Откройте https://этонесамыйдлинныйдомен.рф
✅ Должны отобразиться события
```

### 2. Проверить Property Bot

```
Telegram:
1. Нажмите "🏠 Поиск новостройки"
✅ Должно появиться приветствие

2. Нажмите "🔍 Начать поиск"
✅ Должен начаться пошаговый поиск

3. Или напишите: "Ищу 2-комнатную до 10 млн"
✅ Должен сработать Property Agent и извлечь параметры
```

### 3. Проверить разделение агентов

```
Test 1: Calendar request in Calendar mode
User: "Создай встречу завтра"
✅ Должен обработать llm_agent_yandex

Test 2: Property request in Property mode
User: "Ищу квартиру в Бутово"
✅ Должен обработать llm_agent_property

Test 3: Wrong context
User в Property mode: "Создай встречу"
✅ Должен ответить: "Я помогаю только с поиском недвижимости"
```

---

## 📊 Статус

### ✅ Исправлено

- [x] Radicale контейнер запущен
- [x] Календарный бот работает
- [x] События отображаются
- [x] Создан отдельный Property Agent
- [x] Полное разделение промптов
- [x] Логика переключения режимов
- [x] Out-of-scope detection

### ⏳ В разработке

- [ ] Интеграция с реальными фидами недвижимости
- [ ] Yandex Maps API для геокодирования
- [ ] Анализ фото через Vision API
- [ ] HTML страницы для share-ссылок

---

## 📚 Документация

1. **PROPERTY_BOT_IMPLEMENTATION.md** - Полное описание Property Bot
2. **AI_AGENTS_SEPARATION.md** - Архитектура разделения агентов
3. **FIXES_SUMMARY.md** (этот файл) - Отчёт о исправлениях

---

## 🎯 Итог

### Проблема 1: ✅ Решена
Календарный бот полностью восстановлен и работает.

### Проблема 2: ✅ Решена
AI агенты полностью разделены:
- **Calendar Agent** (llm_agent_yandex) - только календарь
- **Property Agent** (llm_agent_property) - только недвижимость
- Никаких пересечений в промптах
- Автоматическое переключение по режиму
- Out-of-scope detection

Оба бота работают независимо и не мешают друг другу! 🎉
