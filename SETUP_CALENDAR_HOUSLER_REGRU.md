# 🚀 Настройка calendar.housler.ru на VPS (Reg.ru DNS)

**Время:** 20 минут
**Цель:** Перенаправить calendar.housler.ru на VPS 91.229.8.221 и настроить веб-приложение

---

## 📋 Что у нас есть

- ✅ **VPS:** 91.229.8.221 (бот работает)
- ✅ **Домен:** housler.ru (на Reg.ru)
- ✅ **Доступ:** Ispmanager панель
- 🎯 **Задача:** Настроить calendar.housler.ru → VPS

---

## 🔧 Шаг 1: Добавьте DNS запись (5 минут)

### 1.1 Войдите в панель управления доменом

1. Откройте: https://www.reg.ru/user/account/#/domains
2. Войдите:
   - Логин: `u3275798`
   - Пароль: `z8JEEvu8za8JPR3q`
3. Найдите домен **housler.ru**

### 1.2 Добавьте A-запись для поддомена

1. Нажмите на домен **housler.ru**
2. Найдите раздел **"DNS-серверы и зона"** или **"Управление зоной DNS"**
3. Нажмите **"Добавить запись"**
4. Заполните:

```
Тип записи:   A
Субдомен:     calendar
IPv4 адрес:   91.229.8.221
TTL:          3600 (или оставьте по умолчанию)
```

5. Нажмите **"Сохранить"**

### 1.3 Проверьте DNS (подождите 5-10 минут)

```bash
# Проверьте, что DNS настроен правильно
dig calendar.housler.ru +short
# Должен вернуть: 91.229.8.221

# Если команда dig недоступна
ping calendar.housler.ru
# Должен показать: 91.229.8.221
```

**⏳ Пока DNS обновляется, переходите к следующему шагу**

---

## 🖥️ Шаг 2: Настройте Nginx на VPS (10 минут)

### 2.1 Подключитесь к VPS

```bash
ssh root@91.229.8.221
# Введите пароль от VPS
```

### 2.2 Создайте Nginx конфигурацию

```bash
cat > /etc/nginx/sites-available/calendar.housler.ru << 'EOF'
server {
    listen 80;
    server_name calendar.housler.ru;

    # Временно для получения SSL сертификата
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    # После получения SSL, этот блок будет редиректить на HTTPS
    location / {
        return 301 https://$server_name$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name calendar.housler.ru;

    # SSL сертификаты (будут добавлены Certbot автоматически)
    # ssl_certificate /etc/letsencrypt/live/calendar.housler.ru/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/calendar.housler.ru/privkey.pem;

    # Проксирование к Flask приложению
    location / {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Health check
    location /health {
        proxy_pass http://localhost:5000/health;
    }
}
EOF
```

### 2.3 Активируйте конфигурацию

```bash
# Создайте симлинк
ln -sf /etc/nginx/sites-available/calendar.housler.ru /etc/nginx/sites-enabled/

# Проверьте конфигурацию
nginx -t

# Перезапустите Nginx
systemctl reload nginx
```

---

## 🔒 Шаг 3: Получите SSL сертификат (3 минуты)

```bash
# Убедитесь, что Certbot установлен
apt-get update
apt-get install -y certbot python3-certbot-nginx

# Получите SSL сертификат
certbot --nginx -d calendar.housler.ru --non-interactive --agree-tos --email your-email@example.com

# Проверьте автопродление
certbot renew --dry-run
```

**После этого Certbot автоматически:**
- ✅ Получит SSL сертификат
- ✅ Обновит Nginx конфигурацию
- ✅ Настроит автопродление

---

## 🔑 Шаг 4: Обновите переменные окружения (2 минуты)

### 4.1 Обновите .env на VPS

```bash
# Откройте .env
nano /root/ai-calendar-assistant/.env

# Найдите и измените строки:
WEBAPP_URL=https://calendar.housler.ru
DOMAIN=calendar.housler.ru
WEBHOOK_URL=https://calendar.housler.ru/webhook

# Сохраните: Ctrl+O, Enter, Ctrl+X
```

### 4.2 Перезапустите приложение

```bash
cd /root/ai-calendar-assistant
docker-compose restart
```

---

## 📱 Шаг 5: Обновите Telegram Menu Button (1 минута)

```bash
# На VPS выполните:
curl -X POST "https://api.telegram.org/bot$(grep TELEGRAM_BOT_TOKEN /root/ai-calendar-assistant/.env | cut -d '=' -f2)/setChatMenuButton" \
-H "Content-Type: application/json" \
-d '{
  "menu_button": {
    "type": "web_app",
    "text": "🗓 Календарь",
    "web_app": {
      "url": "https://calendar.housler.ru"
    }
  }
}'
```

**Ответ должен быть:** `{"ok":true,"result":true}`

---

## 🚀 Шаг 6: Деплой обновлённого веб-приложения (2 минуты)

### 6.1 Настройте SSH ключ (один раз)

**На вашем Mac:**

```bash
# Добавьте SSH ключ (введите пароль VPS один раз)
ssh-copy-id -i ~/.ssh/calendar_deploy.pub root@91.229.8.221
```

### 6.2 Задеплойте обновления

```bash
cd ~/Desktop/AI-Calendar-Project/ai-calendar-assistant
./deploy_updates.sh
```

**Скрипт задеплоит:**
- ✅ `app.py`
- ✅ `app/routes.py`
- ✅ `app/static/index.html` ⭐ (обновлённый веб-апп)
- ✅ `app/static/script.js`
- ✅ `app/static/styles.css`
- ✅ `bot.py`
- ✅ `requirements.txt`

---

## ✅ Шаг 7: Проверьте работу (2 минуты)

### 7.1 Проверьте DNS

```bash
dig calendar.housler.ru +short
# Должен вернуть: 91.229.8.221
```

### 7.2 Проверьте SSL

```bash
curl -I https://calendar.housler.ru/health
# Должен вернуть: HTTP/2 200
```

### 7.3 Проверьте в браузере

1. Откройте: https://calendar.housler.ru
2. Должна загрузиться веб-страница с календарём
3. Проверьте дату (должна быть актуальная, не 30 октября)

### 7.4 Проверьте в Telegram

1. Откройте бота
2. Нажмите кнопку **"🗓 Календарь"** (внизу, рядом с полем ввода)
3. Должен открыться веб-апп на **calendar.housler.ru**
4. Попробуйте добавить TODO:
   ```
   TODO купить молоко
   ```

---

## 🎯 Итоговая проверка

- [ ] DNS настроен: `calendar.housler.ru` → `91.229.8.221`
- [ ] SSL работает: `https://calendar.housler.ru` открывается
- [ ] Nginx проксирует на Flask: `/health` возвращает 200
- [ ] Веб-апп загружается в браузере
- [ ] Telegram Menu Button открывает веб-апп
- [ ] TODO работает в веб-апе
- [ ] Бот корректно обрабатывает "потом" и "задачи"

---

## 🔍 Troubleshooting

### DNS не обновился

```bash
# Проверьте nameservers для домена
dig NS housler.ru

# Очистите DNS кэш на Mac
sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder
```

### SSL не работает

```bash
# Проверьте логи Certbot
tail -f /var/log/letsencrypt/letsencrypt.log

# Убедитесь, что порт 80 открыт
ufw status
# Если порт закрыт:
ufw allow 80/tcp
ufw allow 443/tcp
```

### Nginx не проксирует

```bash
# Проверьте логи Nginx
tail -f /var/log/nginx/error.log

# Проверьте, что Flask работает
curl http://localhost:5000/health
```

### Веб-апп не загружается

```bash
# Проверьте, что файлы задеплоены
ssh root@91.229.8.221 "ls -la /root/ai-calendar-assistant/app/static/"

# Должны быть:
# index.html (обновлённый)
# script.js
# styles.css
```

---

## 📊 Время выполнения

| Шаг | Время | Статус |
|-----|-------|--------|
| DNS запись | 5 мин | ⏳ Ждём propagation |
| Nginx конфиг | 10 мин | ⚙️ |
| SSL сертификат | 3 мин | 🔒 |
| .env обновление | 2 мин | 📝 |
| Menu Button | 1 мин | 📱 |
| Деплой веб-апа | 2 мин | 🚀 |
| Проверка | 2 мин | ✅ |
| **ИТОГО** | **~25 минут** | 🎯 |

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:

- ✅ Telegram бот на **91.229.8.221**
- ✅ Веб-приложение на **https://calendar.housler.ru**
- ✅ SSL с автопродлением
- ✅ Всё в одном месте (VPS)
- ✅ TODO работает в веб-апе
- ✅ Множественные события через "потом" работают
- ✅ Команда "задачи" работает

**Следующие шаги:**
- Протестируйте TODO в веб-апе
- Проверьте синхронизацию бота и веб-апа
- При необходимости добавьте резервное копирование БД

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи: `docker-compose logs -f`
2. Проверьте Nginx: `tail -f /var/log/nginx/error.log`
3. Проверьте DNS: `dig calendar.housler.ru +short`
