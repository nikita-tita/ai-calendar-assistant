# 🚀 ДЕПЛОЙ В PRODUCTION - 5 МИНУТ

Простая инструкция для запуска в боевом режиме.

---

## ⚡ Автоматический деплой (3 команды)

```bash
cd /Users/fatbookpro/ai-calendar-assistant/ai-calendar-assistant

# 1. Создайте .env из шаблона
cp env.sms_production.example .env

# 2. Сгенерируйте SECRET_KEY
python3 -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32))"
# Скопируйте результат и вставьте в .env

# 3. Запустите автодеплой
./deploy-production.sh
```

Скрипт автоматически:
- ✅ Проверит конфигурацию
- ✅ Проверит Docker
- ✅ Проверит баланс SMS.ru
- ✅ Соберёт образы
- ✅ Запустит приложение
- ✅ Проверит health check

---

## 📝 Ручной деплой (если нужен контроль)

### Шаг 1: Настройте .env

```bash
cp env.sms_production.example .env
nano .env
```

**Обязательно измените:**

```bash
# 1. Сгенерируйте SECRET_KEY (ОБЯЗАТЕЛЬНО!)
SECRET_KEY=<python3 -c "import secrets; print(secrets.token_urlsafe(32))">

# 2. Ваш SMS.ru API ID уже там:
SMS_PROVIDER=sms.ru
SMS_RU_API_ID=779FBF5C-56D6-6AF8-5C8B-63C2F6CF9C90

# 3. Production режим
APP_ENV=production
DEBUG=False

# 4. Ваш домен
TELEGRAM_WEBAPP_URL=https://your-domain.com
CORS_ORIGINS=https://your-domain.com
```

### Шаг 2: Запустите

```bash
# Создайте директорию для данных
mkdir -p data
chmod 700 data

# Запустите через Docker
docker-compose up -d --build

# Проверьте логи
docker-compose logs -f
```

### Шаг 3: Проверьте

```bash
# Health check
curl http://localhost:8000/health

# Тестовая SMS (придёт на телефон!)
curl -X POST http://localhost:8000/api/auth/sms/request \
  -H "Content-Type: application/json" \
  -d '{"phone": "+79991234567"}'
```

---

## 🔐 Настройка SSL (Nginx + Let's Encrypt)

### Быстрая настройка

```bash
# 1. Установите Nginx
sudo apt update
sudo apt install -y nginx certbot python3-certbot-nginx

# 2. Создайте конфиг
sudo nano /etc/nginx/sites-available/calendar-assistant
```

**Минимальный конфиг:**

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
# 3. Активируйте
sudo ln -s /etc/nginx/sites-available/calendar-assistant /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# 4. Получите SSL
sudo certbot --nginx -d your-domain.com

# Готово! Теперь доступно по HTTPS
```

---

## 🛡️ Безопасность (5 минут)

```bash
# 1. Firewall
sudo apt install -y ufw
sudo ufw allow 22    # SSH
sudo ufw allow 80    # HTTP
sudo ufw allow 443   # HTTPS
sudo ufw enable

# 2. Права на .env
chmod 600 .env
chmod 700 data/

# 3. Fail2Ban (опционально)
sudo apt install -y fail2ban
sudo systemctl enable fail2ban
```

---

## 📊 Мониторинг

### Логи

```bash
# Все логи
docker-compose logs -f

# Только SMS
docker-compose logs -f | grep sms

# Последние 100 строк
docker-compose logs --tail=100

# Nginx логи (если настроен)
sudo tail -f /var/log/nginx/access.log
```

### Статус

```bash
# Docker контейнеры
docker-compose ps

# Использование ресурсов
docker stats

# Дисковое пространство
df -h

# Проверка здоровья
curl http://localhost:8000/health
```

### Баланс SMS.ru

```bash
# Проверить баланс
curl "https://sms.ru/my/balance?api_id=779FBF5C-56D6-6AF8-5C8B-63C2F6CF9C90"

# Или через личный кабинет
open https://sms.ru/
```

---

## 🔄 Обновление

```bash
# 1. Остановите
docker-compose down

# 2. Обновите код (если из git)
git pull origin main

# 3. Пересоберите
docker-compose build --no-cache

# 4. Запустите
docker-compose up -d

# 5. Проверьте
docker-compose logs -f
```

---

## 💾 Бэкапы

### Ручной бэкап

```bash
# Создайте директорию
mkdir -p ~/backups

# Бэкап данных
tar -czf ~/backups/data_$(date +%Y%m%d_%H%M%S).tar.gz data/

# Бэкап .env
cp .env ~/backups/env_$(date +%Y%m%d_%H%M%S)
```

### Автоматический (cron)

```bash
# Создайте скрипт
cat > ~/backup.sh << 'EOF'
#!/bin/bash
cd /opt/ai-calendar-assistant/ai-calendar-assistant
tar -czf ~/backups/data_$(date +%Y%m%d).tar.gz data/
find ~/backups -mtime +30 -delete
EOF

chmod +x ~/backup.sh

# Добавьте в cron (каждый день в 3:00)
(crontab -l 2>/dev/null; echo "0 3 * * * ~/backup.sh") | crontab -
```

---

## ✅ Production Checklist

### Перед запуском

- [ ] `.env` создан и настроен
- [ ] `SECRET_KEY` сгенерирован (32+ символа)
- [ ] `APP_ENV=production`
- [ ] `DEBUG=False`
- [ ] SMS.ru API ID настроен
- [ ] SMS.ru баланс > 100₽
- [ ] Telegram Bot Token настроен
- [ ] Домен настроен

### Безопасность

- [ ] SSL/HTTPS работает
- [ ] Firewall настроен (UFW)
- [ ] `.env` chmod 600
- [ ] `data/` chmod 700
- [ ] Fail2Ban установлен (опционально)

### Мониторинг

- [ ] Логи пишутся
- [ ] Health check работает
- [ ] Бэкапы настроены
- [ ] Алерты настроены (опционально)

### Тестирование

- [ ] Тестовая SMS получена
- [ ] Верификация кода работает
- [ ] JWT токен получен
- [ ] API endpoints отвечают
- [ ] Demo страница открывается

---

## 🧪 Первый тест в production

```bash
# 1. Health check
curl https://your-domain.com/health

# Ожидаемый ответ:
# {"status":"healthy","version":"1.0.0"}

# 2. Запросите SMS на ваш реальный номер
curl -X POST https://your-domain.com/api/auth/sms/request \
  -H "Content-Type: application/json" \
  -d '{"phone": "+79991234567"}'

# Ожидаемый ответ:
# {"success":true,"message":"Код отправлен..."}

# 3. Проверьте телефон - должна прийти SMS!
# Текст SMS:
# "Ваш код для входа в AI Calendar Assistant: 123456
#  Код действителен 5 минут."

# 4. Введите полученный код
curl -X POST https://your-domain.com/api/auth/sms/verify \
  -H "Content-Type: application/json" \
  -d '{"phone": "+79991234567", "code": "123456"}'

# Ожидаемый ответ:
# {
#   "success": true,
#   "message": "Вход выполнен успешно!",
#   "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
#   "user_id": "sms_abc123xyz",
#   "is_new_user": true
# }

# 5. Если получили токен - ВСЁ РАБОТАЕТ! 🎉
```

---

## 🎨 Demo страница

```bash
# Откройте в браузере
open https://your-domain.com/static/sms_auth_demo.html
```

1. Введите ваш телефон
2. Нажмите "Получить код"
3. Получите SMS на телефон
4. Введите код
5. Войдите!

---

## 🆘 Что делать если не работает?

### Проблема: Приложение не запускается

```bash
# Проверьте Docker
docker-compose ps
docker-compose logs

# Проверьте .env
cat .env | grep -E "SECRET_KEY|SMS_PROVIDER|APP_ENV"

# Пересоберите
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: SMS не приходят

```bash
# 1. Проверьте баланс
curl "https://sms.ru/my/balance?api_id=779FBF5C-56D6-6AF8-5C8B-63C2F6CF9C90"

# 2. Проверьте логи
docker-compose logs | grep sms

# 3. Проверьте провайдер
grep SMS_PROVIDER .env
# Должно быть: SMS_PROVIDER=sms.ru
```

### Проблема: SSL не работает

```bash
# Обновите сертификат
sudo certbot renew

# Проверьте Nginx
sudo nginx -t
sudo systemctl restart nginx

# Проверьте порты
sudo netstat -tlnp | grep -E ':80|:443'
```

### Проблема: "Invalid token"

```bash
# Проверьте SECRET_KEY в .env
grep SECRET_KEY .env

# Перезапустите приложение
docker-compose restart

# Запросите новый код
curl -X POST https://your-domain.com/api/auth/sms/request \
  -H "Content-Type: application/json" \
  -d '{"phone": "+79991234567"}'
```

---

## 📈 Оптимизация производительности

### PostgreSQL вместо SQLite

```yaml
# Добавьте в docker-compose.yml
services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_DB: calendar_db
      POSTGRES_USER: calendar_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
```

```bash
# Обновите .env
DATABASE_URL=postgresql://calendar_user:password@postgres:5432/calendar_db
```

### Redis для кодов

```yaml
# Добавьте в docker-compose.yml
services:
  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data

volumes:
  redis_data:
```

```bash
# Обновите .env
REDIS_URL=redis://:password@redis:6379/0
```

---

## 📞 Поддержка

### Документация

- 📚 [Полная документация](ai-calendar-assistant/docs/SMS_AUTHENTICATION.md)
- 🏗️ [Архитектура](ai-calendar-assistant/docs/SMS_ARCHITECTURE.md)
- ⚡ [Быстрый старт](ai-calendar-assistant/docs/QUICKSTART_SMS.md)
- ✅ [Чеклист](CHECKLIST_SMS.md)

### SMS.ru

- 🌐 Личный кабинет: https://sms.ru/
- 📧 Поддержка: support@sms.ru
- 📱 Telegram: @smsru_bot

---

## 🎉 Поздравляем!

Если всё работает - у вас есть:

✅ **Production-ready система**  
✅ **Реальные SMS** (не mock!)  
✅ **JWT авторизация**  
✅ **HTTPS/SSL**  
✅ **Мониторинг**  
✅ **Бэкапы**  

**Стоимость запуска:** ~5₽ (тестовые SMS)  
**Время деплоя:** 5-10 минут  
**Сложность:** Низкая  

---

## 📊 Что дальше?

1. **Интегрируйте в frontend**
   - Примеры для React/Vue готовы
   - Demo код можно использовать

2. **Настройте мониторинг**
   - Prometheus + Grafana
   - Email алерты
   - SMS уведомления

3. **Оптимизируйте**
   - PostgreSQL
   - Redis
   - CDN для статики

4. **Масштабируйте**
   - Несколько инстансов
   - Load balancer
   - Auto-scaling

---

<div align="center">

## 🚀 ГОТОВО К БОЮ!

**Версия:** 1.0.0  
**Дата:** 22 декабря 2025  
**Статус:** ✅ Production Ready

Made with ❤️ in Russia 🇷🇺

[📚 Документация](PRODUCTION_DEPLOYMENT.md) • [🤖 Автодеплой](ai-calendar-assistant/deploy-production.sh) • [✅ Чеклист](CHECKLIST_SMS.md)

</div>
