# ⚡ Быстрый старт Admin Panel v2

**5 минут до запуска!**

---

## 🎯 ЧТО СДЕЛАНО

✅ **Новая система аутентификации:**
- Login + Password вместо 3 паролей
- 2FA (Google Authenticator)
- httpOnly cookies (безопасно)
- Panic password (fake mode сохранен)

✅ **Файлы созданы:**
- `app/models/admin_user.py` - модели данных
- `app/services/admin_auth_service.py` - сервис аутентификации
- `app/routers/admin_v2.py` - новые API роуты
- `scripts/migrate_admin_to_v2.py` - скрипт миграции
- `DEPLOYMENT_GUIDE_V2.md` - полное руководство
- `ADMIN_IMPROVEMENTS_PLAN.md` - детальный план
- `ADMIN_COMPARISON.md` - сравнение версий

✅ **Зависимости обновлены:**
- `bcrypt` - хеширование паролей
- `pyotp` - 2FA (TOTP)
- `qrcode` - QR коды для 2FA
- `slowapi` - rate limiting

---

## 🚀 ЗАПУСК ЗА 3 ШАГА

### 1. Установить зависимости (30 сек)

```bash
cd /Users/fatbookpro/ai-calendar-assistant/ai-calendar-assistant
pip install bcrypt==4.1.1 pyotp==2.9.0 qrcode[pil]==7.4.2 slowapi==0.1.9
```

### 2. Запустить миграцию (1 мин)

```bash
# Проверить что будет сделано
python scripts/migrate_admin_to_v2.py --dry-run

# Применить изменения
python scripts/migrate_admin_to_v2.py
```

### 3. Обновить main.py (2 мин)

Добавить новый роутер в `app/main.py`:

```python
# В секции импортов добавить:
from app.routers import admin_v2

# В секции роутеров добавить:
app.include_router(admin_v2.router, prefix="/api/admin/v2", tags=["admin_v2"])
```

**Готово!** Теперь можно запустить сервер и войти.

---

## 🔐 ПЕРВЫЙ ВХОД

### 1. Запустить сервер

```bash
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. Открыть админку

```
http://localhost:8000/static/admin.html
```

### 3. Войти

```
Username: admin
Password: <ваш ADMIN_PASSWORD_1 из .env>
2FA Code: (оставить пустым при первом входе)
```

### 4. Настроить 2FA

- Отсканировать QR код в Google Authenticator
- Ввести 6-значный код
- Готово!

---

## 📝 ЧТО ДАЛЬШЕ

### Фаза 1 (MVP) - ГОТОВО ✅
- ✅ Login/Password + 2FA
- ✅ httpOnly cookies
- ✅ Panic password (fake mode)
- ✅ Audit log
- ✅ Rate limiting

### Фаза 2 (Стандарт) - СЛЕДУЮЩИЙ ШАГ
- ⏳ Обновить UI (новая форма входа)
- ⏳ Добавить роли (admin, viewer, moderator)
- ⏳ Поиск и фильтры
- ⏳ Экспорт данных (CSV)

### Фаза 3 (Максимум) - ОПЦИОНАЛЬНО
- ⏳ Email уведомления
- ⏳ Telegram интеграция
- ⏳ Расширенная аналитика

---

## 🔧 НАСТРОЙКА .env

Минимум что нужно:

```env
# Основной пароль (обязательно)
ADMIN_PASSWORD_1=your_secure_password_123!

# Email админа
ADMIN_EMAIL=nikitatitov070@yandex.ru

# Опционально: Panic password для fake mode
ADMIN_PASSWORD_2=your_panic_password_456!
```

---

## 🧪 ТЕСТИРОВАНИЕ

```bash
# Проверить что миграция прошла
sqlite3 analytics.db "SELECT username, email, role FROM admin_users;"

# Должно вывести:
# admin|nikitatitov070@yandex.ru|admin

# Проверить API
curl http://localhost:8000/api/admin/v2/health

# Должно вернуть:
# {"status":"ok","version":"v2"}
```

---

## 📚 ДОКУМЕНТАЦИЯ

- **`DEPLOYMENT_GUIDE_V2.md`** - полное руководство по развертыванию
- **`ADMIN_IMPROVEMENTS_PLAN.md`** - детальный план всех улучшений
- **`ADMIN_COMPARISON.md`** - сравнение старой и новой системы

---

## ❓ ВОПРОСЫ

### Как работает panic password?

- **Обычный пароль (ADMIN_PASSWORD_1)** → нормальный доступ
- **Panic password (ADMIN_PASSWORD_2)** → fake mode (пустая статистика)

Используй panic password если кто-то заставляет тебя войти в админку.

### Что если забыл пароль?

1. Открыть `.env` файл
2. Посмотреть `ADMIN_PASSWORD_1`
3. Или изменить его и перезапустить миграцию

### Как добавить еще админов?

Пока нельзя через UI. В Фазе 2 добавим:
- Страницу управления админами
- Роли (admin, viewer, moderator)
- Приглашения по email

### Как отключить 2FA?

Не рекомендуется! Но если нужно:

```sql
sqlite3 analytics.db "UPDATE admin_users SET totp_enabled=0 WHERE username='admin';"
```

### Старая админка еще работает?

Да! Старые роуты `/api/admin/*` работают как раньше.
Новые роуты: `/api/admin/v2/*`

Когда протестируешь v2 - можно переключить полностью.

---

## 🎉 ГОТОВО!

Теперь у тебя:
- ✅ Удобный вход (login/password)
- ✅ Безопасность (2FA + httpOnly cookies)
- ✅ Panic mode (fake password)
- ✅ Аудит (кто что делал)
- ✅ Масштабируемость (можно добавлять админов)

**Следующий шаг:** Обновить UI (новая форма входа) - см. Фазу 2

**Вопросы?** Читай `DEPLOYMENT_GUIDE_V2.md` или спрашивай!

---

**Удачи! 🚀**

