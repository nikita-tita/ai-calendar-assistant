# 🎉 AI Calendar Bot - Финальная инструкция

## ✅ Что установлено и работает

### 🤖 Telegram Bot
- **Статус:** ✅ Запущен и работает
- **Токен:** `***REDACTED_BOT_TOKEN***`
- **Версия:** Полная (с Whisper через OpenAI API, Anthropic Claude, CalDAV)
- **Контейнер:** `telegram-bot`

### 📅 Radicale Calendar Server
- **Статус:** ✅ Запущен и работает
- **URL:** http://91.229.8.221:5232
- **Логин:** `admin`
- **Пароль:** `admin123`
- **Контейнер:** `radicale`

### 🖥️ Сервер REG.RU
- **IP:** 91.229.8.221
- **ОС:** Ubuntu 22.04 LTS
- **Логин:** root
- **Пароль:** upvzrr3LH4pxsaqs
- **Свободно места:** ~6.8GB

---

## 🚀 Как пользоваться ботом

### В Telegram:

1. Найдите вашего бота по токену или имени
2. Отправьте `/start`
3. Бот готов к работе!

**Команды:**
- `/start` - Приветствие и список команд
- `/help` - Справка
- Просто напишите событие, например: "Встреча завтра в 15:00"

---

## 📋 Управление на сервере

### Подключение к серверу:

```bash
ssh root@91.229.8.221
# Пароль: upvzrr3LH4pxsaqs
```

### Основные команды:

```bash
cd /root/ai-calendar-assistant

# Просмотр статуса всех сервисов
docker ps

# Логи бота
docker logs -f telegram-bot

# Логи календаря
docker logs -f radicale

# Перезапуск бота
docker-compose -f docker-compose.full.yml restart telegram-bot

# Перезапуск календаря
docker-compose -f docker-compose.full.yml restart radicale

# Перезапуск всего
docker-compose -f docker-compose.full.yml restart

# Остановка всего
docker-compose -f docker-compose.full.yml down

# Запуск всего
docker-compose -f docker-compose.full.yml up -d
```

---

## ⚙️ Настройки

### Файл .env (настройки бота):

```bash
nano /root/ai-calendar-assistant/.env
```

**Важные параметры:**
- `TELEGRAM_BOT_TOKEN` - токен бота
- `OPENAI_API_KEY` - для распознавания голоса (Whisper)
- `ANTHROPIC_API_KEY` - для AI обработки (Claude)
- `RADICALE_URL` - адрес календаря (сейчас `http://radicale:5232`)
- `RADICALE_USERNAME` - логин календаря (сейчас `admin`)
- `RADICALE_PASSWORD` - пароль календаря (сейчас `admin123`)

После изменений:
```bash
docker-compose -f docker-compose.full.yml restart
```

### Настройки календаря Radicale:

```bash
nano /root/ai-calendar-assistant/radicale_config/config
```

Файл пользователей:
```bash
nano /root/ai-calendar-assistant/radicale_config/users
```

---

## 🌐 Доступ к календарю через браузер/приложения

### Веб-интерфейс:
http://91.229.8.221:5232

Логин: `admin`
Пароль: `admin123`

### Подключение из календарных приложений:

**Apple Calendar (iOS/Mac):**
1. Настройки → Учётные записи → Добавить учётную запись
2. Выбрать "Другое" → "CalDAV"
3. Сервер: `91.229.8.221:5232`
4. Имя пользователя: `admin`
5. Пароль: `admin123`

**Google Calendar / Thunderbird:**
- CalDAV URL: `http://91.229.8.221:5232/admin/`

---

## 🔧 Обслуживание

### Обновление кода бота:

```bash
ssh root@91.229.8.221
cd /root/ai-calendar-assistant

# Остановить
docker-compose -f docker-compose.full.yml down

# Обновить код (если используете Git)
git pull

# Или скопировать новые файлы с локальной машины
# (см. инструкцию ниже)

# Пересобрать и запустить
docker-compose -f docker-compose.full.yml up -d --build
```

### Копирование обновлённого кода с локальной машины:

```bash
# На вашем Mac:
cd /Users/fatbookpro/ai-calendar-assistant

rsync -avz --progress \
  --exclude='.git' \
  --exclude='__pycache__' \
  --exclude='*.pyc' \
  app/ root@91.229.8.221:/root/ai-calendar-assistant/app/

rsync -avz run_polling.py root@91.229.8.221:/root/ai-calendar-assistant/

# Затем на сервере:
ssh root@91.229.8.221
cd /root/ai-calendar-assistant
docker-compose -f docker-compose.full.yml restart
```

### Backup календаря:

```bash
# На сервере
cd /root/ai-calendar-assistant
tar -czf radicale_backup_$(date +%Y%m%d).tar.gz radicale_data/

# Скачать на локальную машину
# На Mac:
scp root@91.229.8.221:/root/ai-calendar-assistant/radicale_backup_*.tar.gz ~/Desktop/
```

### Восстановление из backup:

```bash
# На сервере
cd /root/ai-calendar-assistant
docker-compose -f docker-compose.full.yml down
rm -rf radicale_data/
tar -xzf radicale_backup_YYYYMMDD.tar.gz
docker-compose -f docker-compose.full.yml up -d
```

---

## 📊 Мониторинг

### Проверка использования ресурсов:

```bash
# Использование контейнерами
docker stats

# Место на диске
df -h

# Память
free -h

# Размер логов
du -sh /root/ai-calendar-assistant/logs/
```

### Очистка места:

```bash
# Очистка Docker
docker system prune -a

# Удаление старых логов
rm -rf /root/ai-calendar-assistant/logs/*.log

# Очистка логов Docker
journalctl --vacuum-time=7d
```

---

## 🐛 Решение проблем

### Бот не отвечает:

```bash
# Проверить статус
docker ps

# Посмотреть логи
docker logs --tail 50 telegram-bot

# Перезапустить
docker restart telegram-bot
```

### Календарь не работает:

```bash
# Проверить статус
docker ps | grep radicale

# Логи
docker logs radicale

# Проверить права
chmod -R 777 /root/ai-calendar-assistant/radicale_data

# Перезапустить
docker restart radicale
```

### Конфликт токенов (409 Conflict):

Это значит где-то ещё запущен бот с тем же токеном. Создайте новый токен:

1. Telegram → @BotFather
2. `/mybots` → выбрать бота → API Token → Revoke
3. Скопировать новый токен
4. Обновить на сервере:

```bash
nano /root/ai-calendar-assistant/.env
# Изменить TELEGRAM_BOT_TOKEN
docker-compose -f docker-compose.full.yml restart
```

---

## 🔐 Безопасность

### Рекомендуемые действия:

1. **Смените пароль календаря:**
   ```bash
   # Сгенерировать новый bcrypt hash
   htpasswd -bnBC 10 "" "новый_пароль" | tr -d ':\n'

   # Обновить файл
   nano /root/ai-calendar-assistant/radicale_config/users
   # admin:новый_hash

   docker restart radicale
   ```

2. **Настройте firewall:**
   ```bash
   ufw allow 22/tcp    # SSH
   ufw allow 5232/tcp  # Radicale (если нужен внешний доступ)
   ufw enable
   ```

3. **Настройте SSL (рекомендуется для production):**
   - Получите домен
   - Настройте Nginx с Let's Encrypt
   - Проксируйте Radicale через HTTPS

---

## 📁 Структура файлов

```
/root/ai-calendar-assistant/
├── app/                          # Код бота
├── logs/                         # Логи бота
├── credentials/                  # Учётные данные
├── radicale_data/               # Данные календаря
├── radicale_config/             # Конфиг календаря
│   ├── config                   # Настройки Radicale
│   └── users                    # Пользователи (htpasswd)
├── .env                         # Переменные окружения
├── docker-compose.full.yml      # Docker Compose конфиг
├── Dockerfile.production        # Dockerfile бота
├── requirements-full.txt        # Зависимости Python
└── run_polling.py              # Точка входа бота
```

---

## 💰 Стоимость

**REG.RU VPS:** ~200₽/месяц

**Что входит:**
- Telegram бот 24/7
- CalDAV календарь
- 512MB RAM + 1GB swap
- 10GB SSD (используется ~2.6GB)

---

## 📞 Поддержка

### REG.RU:
- Телефон: 8 800 333-33-33
- Email: support@reg.ru
- Личный кабинет: https://www.reg.ru/user/account

### Telegram Bot API:
- Документация: https://core.telegram.org/bots/api
- @BotFather для управления ботом

### Radicale:
- Документация: https://radicale.org/v3.html
- GitHub: https://github.com/Kozea/Radicale

---

## 🎯 Быстрая справка

```bash
# Подключение
ssh root@91.229.8.221

# Статус
docker ps

# Логи бота
docker logs -f telegram-bot

# Логи календаря
docker logs -f radicale

# Перезапуск
docker-compose -f /root/ai-calendar-assistant/docker-compose.full.yml restart

# Остановка
docker-compose -f /root/ai-calendar-assistant/docker-compose.full.yml down

# Запуск
docker-compose -f /root/ai-calendar-assistant/docker-compose.full.yml up -d

# Обновление
cd /root/ai-calendar-assistant && docker-compose -f docker-compose.full.yml up -d --build
```

---

## ✅ Чеклист работоспособности

- [x] Бот отвечает в Telegram
- [x] Нет конфликтов (409 Conflict)
- [x] Календарь доступен по http://91.229.8.221:5232
- [x] Оба контейнера имеют статус "healthy" или "Up"
- [x] Логи без критических ошибок
- [x] Бот подключён к локальному Radicale

**Всё работает! 🎉**

---

**Документация создана:** 2025-10-10
**Версия:** 1.0 (Production)
**Сервер:** REG.RU VPS (91.229.8.221)
